// @betravedur/domain — public barrel.
// Task 1 seeds this with a version const so the smoke test has something to import.
// Task 2 rewrites this to re-export every domain module.
export const DOMAIN_VERSION = "0.1.0";
