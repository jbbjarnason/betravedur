---
phase: 02-derived-data-pipeline-backfill
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - pipeline/src/aggregate.ts
  - pipeline/src/index.ts
  - pipeline/test/aggregate.test.ts
  - PIPELINE.md
  - package.json
autonomous: true
requirements: [DATA-02, DATA-04, DATA-07]
tags: [pipeline, aggregate, orchestrator, data-branch, git, squash-reset, live-run, e2e]

must_haves:
  truths:
    - "aggregate.ts orchestrates: read raw NDJSON for touched stations -> encodeDerived (season-year via groupBySeasonYear on decode) -> write derived/{station}.{hash}.json -> update manifest.json -> regenerate stations.json, re-deriving only changed stations"
    - "A full-chain offline test proves raw fixture rows -> rawstore -> aggregate -> derived file -> decode -> domain math equals the direct domain path (incl. a wrapping Dec->Jan window)"
    - "The orphan `data` branch exists (created via the git 2.39 two-step worktree recipe), holds raw/ + derived/ + stations.json + manifest.json, and leaves the `main` worktree undisturbed"
    - "A real subset backfill run (2-3 stations, full history) completes: high-water marks advance, a resume-after-interrupt fetches only newer years, and measured derived sizes are recorded (AWS tens of KB; deep SYNOP up to ~200KB, documented exception)"
    - "PIPELINE.md documents chunk/pace/retry policy, the two-step orphan recipe (+ CI one-liner), squash-reset, field-pruning, the deep-station size exception, and the data-branch force-push-owned-by-pipeline rule"
  artifacts:
    - path: "pipeline/src/aggregate.ts"
      provides: "aggregate CLI: raw -> derived + manifest + stations.json (touched-only)"
      exports: ["aggregateStation", "main"]
      min_lines: 60
    - path: "PIPELINE.md"
      provides: "operator doc: backfill policy, data-branch mechanics, squash-reset, size exception"
      min_lines: 40
    - path: "pipeline/test/aggregate.test.ts"
      provides: "full-chain raw->derived->decode->domain round-trip incl. wrapping window"
  key_links:
    - from: "pipeline/src/aggregate.ts"
      to: "pipeline/src/derive.ts"
      via: "encodeDerived"
      pattern: "encodeDerived"
    - from: "pipeline/src/aggregate.ts"
      to: "pipeline/src/manifest.ts"
      via: "updateManifest / contentHash"
      pattern: "updateManifest|contentHash"
    - from: "pipeline/src/aggregate.ts"
      to: "@betravedur/domain"
      via: "groupBySeasonYear on decoded rows"
      pattern: "groupBySeasonYear"
---

<objective>
Tie the pieces into one runnable pipeline and prove it on real data. `aggregate.ts` reads the field-pruned raw store, re-derives only touched stations into content-hashed `derived/*.json`, and rewrites `manifest.json` + `stations.json`. Then create the orphan `data` branch (git 2.39 two-step recipe), run a real subset backfill + aggregate against live api.vedur.is for 2-3 stations to full history, self-verify high-water/resume/idempotency and record measured sizes, and write `PIPELINE.md` documenting the whole operational contract Phase 8 will schedule.

Purpose: This is the phase's closing vertical slice — the first task in this plan makes "derived files exist from real raw data" true end-to-end offline; the live task makes DATA-02/DATA-04/DATA-07 observably true on the real network and on a real `data` branch. Per the no-review directive, Claude runs the live commands, inspects output, and records evidence in the SUMMARY (no human checkpoint).

Output: `aggregate.ts` orchestrator + full-chain test; the orphan `data` branch populated with a real subset backfill; `PIPELINE.md`; `npm run aggregate` wired; measured sizes + git integrity evidence in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-derived-data-pipeline-backfill/02-RESEARCH.md
@.planning/phases/02-derived-data-pipeline-backfill/02-CONTEXT.md

<interfaces>
<!-- Built in Plans 01/02/03 — orchestrate these, do not reimplement. -->
```typescript
// pipeline/src/derive.ts (Plan 01)
export function encodeDerived(rows: DailyObservation[], type: StationType): DerivedFile;
export function decodeDerived(d: DerivedFile): DailyObservation[];
// pipeline/src/backfill.ts (Plan 02)
export function backfillStation(kind: "aws"|"synop", id: number, startYear: number): Promise<number>;
// pipeline/src/rawstore.ts (Plan 02)
export function upsertPartition(root, station, rows): void;
export function readPartition(root, station, year): DailyObservation[];
export function highWaterYear(root, station): number | null;
// pipeline/src/manifest.ts (Plan 03)
export function contentHash(bytes): string;
export function updateManifest(manifest, station, derivedBytes, marks): Manifest;
export function readManifest(path): Manifest; export function serializeManifest(m): string;
// pipeline/src/stations.ts (Plan 03)
export function buildStationsJson(stations, qualifyingCounts): StationsJson;
// @betravedur/domain
export function groupBySeasonYear(rows, spec): Map<number, DailyObservation[]>;
export function qualifyingYears(...): number[]; export function effectiveN(q): {n,sufficient};
```
</interfaces>

# Verified git recipe (git 2.39.5 on this machine — 02-RESEARCH executed these)
Two-step orphan (worktree add --orphan is git >=2.42; NOT available here):
```
git worktree add --detach ../betravedur-data
cd ../betravedur-data
git checkout --orphan data
git rm -rf . 2>/dev/null || true
mkdir -p raw derived
# pipeline writes raw/ derived/ stations.json manifest.json
git add -A && git commit -m "data: initial subset backfill $(date +%F)"
```
Verified properties: `main` worktree undisturbed; a branch is checked out in only one worktree at a time; squash-reset (checkout --orphan data-fresh -> commit -> delete/rename data -> force-push) collapses history. CI (git >=2.42) may use `git worktree add --orphan data ./data-wt` — document both.

# Locked size honesty (02-RESEARCH / orchestrator decision)
- Per-station-year budget: <=4KB gzip/station-year enforced (test in Plan 01).
- Deep SYNOP stations (~8 century-deep): accept ~200KB derived files; document as an explicit exception, NOT a bug. Do NOT cap history (contradicts "backfill full history").
- Only derived/ + stations.json + manifest.json ship to Pages; the raw store NEVER ships to dist/.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the aggregate orchestrator + full-chain round-trip test</name>
  <files>pipeline/src/aggregate.ts, pipeline/src/index.ts, pipeline/test/aggregate.test.ts, package.json</files>
  <read_first>
    - pipeline/src/derive.ts (encodeDerived/decodeDerived — Plan 01)
    - pipeline/src/rawstore.ts (readPartition/highWaterYear — Plan 02)
    - pipeline/src/manifest.ts (updateManifest/contentHash/serializeManifest — Plan 03)
    - pipeline/src/stations.ts (buildStationsJson — Plan 03)
    - packages/domain/src/window.ts (groupBySeasonYear)
    - .planning/phases/02-derived-data-pipeline-backfill/02-RESEARCH.md (System Architecture Diagram; "Bake-the-invariant" pattern; DATA-04 row)
  </read_first>
  <behavior>
    - Test A (full-chain round-trip): seed a tmp raw store via `upsertPartition` with hand-authored multi-year rows for one AWS and one SYNOP station (incl. Dec/Jan boundary rows); run `aggregateStation` for each; read back the emitted derived file, `decodeDerived` it, and assert `groupBySeasonYear(decoded, wrappingSpec)` per-season N + mean equals the direct domain path on the original rows — for BOTH a non-wrapping and a wrapping {startDoy:364,endDoy:3} window. (Extends Plan 01's derive round-trip through the real read->encode->write->read path.)
    - Test B (touched-only re-derivation): after aggregating, re-running `aggregate` with NO raw change leaves derived files + manifest byte-identical (no spurious re-hash); changing one station's raw rows re-derives ONLY that station (its manifest hash changes; others unchanged).
    - Test C (outputs present): after aggregation, `manifest.json` indexes each station to its `derived/{station}.{hash}.json` and `stations.json` contains the qualifying stations; AWS derived file omits `r`, SYNOP omits `dv`.
    - Test D (raw never in outputs): the aggregate output set written for shipping contains only derived/ + stations.json + manifest.json — assert `raw/` is not among the ship-listed outputs.
  </behavior>
  <action>
    Create `pipeline/src/aggregate.ts`:
    - `aggregateStation(root, station, type, manifest)`: read all year partitions via `readPartition`/`highWaterYear`; concat rows; `encodeDerived(rows, type)`; `JSON.stringify` -> bytes; `updateManifest(manifest, station, bytes, {from,to,lastFetched})`; write `derived/{station}.{hash}.json` (only when the hash changed — touched-only). Compute the station's qualifying-years count (via domain `qualifyingYears`/`effectiveN`) for the stations.json filter.
    - `main()` CLI (invokable `npm run aggregate`): determine touched stations (from a station list arg or all present raw stations), aggregate each, `buildStationsJson(registry, qualifyingCounts)`, write `stations.json` + `serializeManifest`. Season-year is applied at DECODE/aggregation time via `groupBySeasonYear` (calendar-year storage per Plan 01 convention) — reuse `@betravedur/domain`, do NOT reimplement.
    - `pipeline/src/index.ts`: barrel re-exporting the public pipeline surface (encode/decode, backfillStation, aggregate main) so the workspace has a clean entry.
    - Confirm root `package.json` `scripts.aggregate` and `scripts.backfill` resolve (declared in Plan 01); adjust if the CLI arg shape differs.
    TDD RED->GREEN: write `aggregate.test.ts` (Tests A-D) against a tmp store first.
  </action>
  <verify>
    <automated>npx vitest run pipeline -t "aggregate" && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run pipeline -t "aggregate"` green covering full-chain round-trip (non-wrapping AND wrapping window), touched-only re-derivation, outputs present, raw-never-shipped.
    - Test A asserts a wrapping {startDoy:364,endDoy:3} window round-trips through the on-disk raw->derived path (grep test for `364`).
    - `aggregate.ts` imports `encodeDerived` (Plan 01), `updateManifest`/`contentHash` (Plan 03), and `groupBySeasonYear` (domain) — grep confirms all three; it does NOT reimplement quantization or season-year grouping.
    - Touched-only: Test B asserts unchanged raw -> byte-identical manifest, changed raw -> only that station's hash changes.
    - Full monorepo suite `npx vitest run` green; `npx tsc -p pipeline/tsconfig.json --noEmit` clean.
  </acceptance_criteria>
  <done>The aggregate orchestrator turns the raw store into content-hashed derived files + manifest + stations.json, re-deriving only touched stations, proven end-to-end offline including the season-year wrap.</done>
</task>

<task type="auto">
  <name>Task 2: Create the orphan data branch, run a real subset backfill+aggregate, and write PIPELINE.md</name>
  <files>PIPELINE.md</files>
  <read_first>
    - .planning/phases/02-derived-data-pipeline-backfill/02-RESEARCH.md ("`data` Branch Mechanics — Verified git Commands"; Runtime estimate; Data Volume measured sizes; Pitfall 7 git-version skew)
    - .planning/phases/02-derived-data-pipeline-backfill/02-VALIDATION.md (Manual-Only Verifications — self-verified per no-review directive)
    - .planning/STATE.md (no-human-checkpoint directive: run the commands, inspect output, record evidence)
  </read_first>
  <action>
    This is a live, side-effecting task. Per the STATE no-review directive, Claude runs the commands, inspects real output, cross-checks acceptance criteria, and records evidence in the SUMMARY — no human checkpoint.
    1. Create the orphan `data` branch with the VERIFIED git 2.39 two-step recipe (worktree add --detach ../betravedur-data, cd, checkout --orphan data, git rm -rf ., mkdir -p raw derived). Confirm the `main` worktree is undisturbed afterward (`git -C <main> status` clean, files intact).
    2. Run a REAL subset backfill for 2-3 stations covering FULL history via `npm run backfill` (or `tsx pipeline/src/backfill.ts`): pick Keflavík AWS #1350 (shallow, ~21yr) and Reykjavík SYNOP #1 (deep, ~77yr) at minimum — these are the measured shallow+deep extremes — writing partitions into the data worktree's `raw/`. Then run `npm run aggregate` to emit `derived/`, `stations.json`, `manifest.json`.
    3. Self-verify DATA-02 behaviors: (a) high-water marks in manifest.json advance to the latest fetched year; (b) INTERRUPT-and-resume — after a complete run, re-run backfill and confirm it fetches only newer years (near-zero new requests) and the raw partitions are byte-identical (idempotent); (c) confirm no duplicate (station,date) rows in a partition (row count <=366/year).
    4. Record measured derived sizes (gzip + brotli via Node `zlib`) per station: confirm AWS is tens of KB and the deep SYNOP station is ~150-200KB — the documented exception, not a failure. Confirm the raw store per-row byte rate is ~110 B/row (field-pruned), not ~580 B/row (unpruned).
    5. Commit raw/ + derived/ + stations.json + manifest.json to the `data` branch (`git add -A && git commit -m "data: initial subset backfill <date>"`). Do NOT push unless the environment has a remote configured and it is safe; if pushing, the `data` branch is force-push-owned by the pipeline and never has PRs (document this either way).
    6. Verify the squash-reset recipe works locally on the 2-commit data branch (collapses to 1 commit, working tree preserved) — do this on the real branch or a throwaway to confirm the documented recipe, and note the result.
    7. Write `PIPELINE.md` at repo root documenting: backfill chunk/pace/retry policy (5 station-years, <=4 req/s, 413-halve/502-503-retry/404-empty), the two-step orphan recipe AND the CI git>=2.42 one-liner, the incremental-update recipe, the periodic squash-reset recipe (force-push-owned), field-pruning (10 fields), the derived size budget (<=4KB/station-year) with the deep-SYNOP ~200KB documented exception, and the rule that only derived/+stations.json+manifest.json ship to Pages (raw never ships).
    Put ALL measured numbers and git-integrity evidence (command outputs) into the SUMMARY.
  </action>
  <verify>
    <automated>test -f PIPELINE.md && node -e "const t=require('fs').readFileSync('PIPELINE.md','utf8'); const need=['checkout --orphan','squash','req/s','station-year','field-prun']; const miss=need.filter(k=>!new RegExp(k,'i').test(t)); if(miss.length){console.error('PIPELINE.md missing:',miss);process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - The orphan `data` branch exists (`git branch -a` lists `data`) and was created without disturbing `main` (evidence: `main` status clean before/after, recorded in SUMMARY).
    - A real subset backfill of >=2 stations (incl. the deep SYNOP #1 and shallow AWS #1350) produced `raw/`, `derived/`, `stations.json`, `manifest.json` on the data branch (evidence: file listing + row/size counts in SUMMARY).
    - DATA-02 self-verification evidence in SUMMARY: high-water advance, resume-fetches-only-newer (idempotent re-run, byte-identical partitions), no duplicate (station,date).
    - Measured derived sizes recorded: AWS station tens of KB; deep SYNOP ~150-200KB flagged as the documented exception. Raw store ~110 B/row (pruned).
    - Squash-reset recipe verified to collapse history with the working tree preserved (evidence in SUMMARY).
    - `PIPELINE.md` exists and contains: two-step orphan recipe, CI one-liner, squash-reset, chunk/pace/retry policy, field-pruning, size budget + deep-station exception, ship-only-derived rule (automated grep gate above passes).
  </acceptance_criteria>
  <done>The `data` branch holds a real subset backfill with verified resume/idempotency and recorded sizes, and PIPELINE.md documents the full operational contract for Phase 8.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| api.vedur.is -> live backfill | Real network; schema-asserted (Phase 1); error taxonomy handled (Plan 02). |
| pipeline -> git `data` branch | Side-effecting git; must never force-push `main`; orphan branch is single-writer, force-push-owned by the pipeline. |
| data branch -> Pages build (Phase 3/8) | Only derived/+stations.json+manifest.json ship; raw never ships. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Tampering | force-push clobbers `main` | mitigate | Never force-push main; squash-reset force-pushes ONLY the orphan `data` branch; two-step recipe leaves main worktree untouched (verified). Documented in PIPELINE.md. |
| T-02-11 | Availability | `.git` DoS via unbounded data history | mitigate | Orphan branch + periodic squash-reset (verified recipe) + field-pruned raw store (DATA-07). |
| T-02-12 | Tampering | live 502 recorded as permanent no-data during real run | mitigate | Plan 02 taxonomy (503/502 retryable, never `[]`); self-verify no missing years for stations with data. |
| T-02-13 | Info disclosure | secret committed to data branch | accept | Nothing secret fetched/stored; API keyless; no env/secrets touched (02-RESEARCH State Inventory). |
| T-02-SC | Tampering | npm installs | accept | No new packages; Node fs/zlib/crypto/fetch + tsx only (02-RESEARCH legitimacy audit: none new). |
</threat_model>

<verification>
- `npx vitest run` full monorepo suite green (incl. season-year round-trip + size budget).
- `BETRA_LIVE`-gated smoke: fetch one real station-year and re-derive it matches (matches Phase 1 live-gated pattern) — run once, evidence in SUMMARY.
- Manual-but-self-verified (no human checkpoint): data-branch integrity, subset backfill resume/idempotency, measured sizes — all recorded in SUMMARY.
</verification>

<success_criteria>
- `aggregate` turns the raw store into content-hashed derived files + manifest + stations.json, touched-only, proven end-to-end incl. season-year wrap.
- The orphan `data` branch exists with a real subset backfill; resume/idempotency verified; main undisturbed.
- Measured sizes recorded (AWS tens of KB; deep SYNOP ~200KB documented exception).
- PIPELINE.md documents backfill policy, data-branch mechanics, squash-reset, size exception, and ship-only-derived rule.
</success_criteria>

<output>
Create `.planning/phases/02-derived-data-pipeline-backfill/02-04-SUMMARY.md` when done.
</output>
