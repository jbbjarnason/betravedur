---
phase: 01-data-access-domain-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.base.json
  - vitest.config.ts
  - .gitignore
  - .nvmrc
  - packages/domain/package.json
  - packages/domain/tsconfig.json
  - packages/domain/src/types.ts
  - packages/domain/src/index.ts
  - packages/domain/src/window.ts
  - packages/domain/src/coverage.ts
  - packages/domain/src/wind.ts
  - packages/domain/src/precip.ts
  - packages/domain/src/score.ts
  - packages/domain/src/attribution.ts
  - packages/domain/test/smoke.test.ts
  - packages/fetch/package.json
  - packages/fetch/tsconfig.json
  - packages/fetch/src/client.ts
  - packages/fetch/src/observations.ts
  - packages/fetch/src/stations.ts
  - scripts/skeleton-demo.ts
  - test/e2e/skeleton.test.ts
autonomous: false
requirements: [DATA-01]
user_setup: []

must_haves:
  truths:
    - "npm install + npx vitest run succeeds on a fresh clone (workspace + test infra work)"
    - "A single command fetches real daily observations for 2-3 stations from api.vedur.is and prints a human-readable result to stdout"
    - "The fetched real rows pass through the domain package (window selection + circular mean stub + score stub) with no crash, proving the end-to-end chain"
    - "@betravedur/domain has zero runtime dependencies and imports cleanly in both Node and a browser-target tsconfig"
  artifacts:
    - path: "package.json"
      provides: "npm workspaces root, type:module, workspaces:[packages/*]"
      contains: "workspaces"
    - path: "packages/domain/src/types.ts"
      provides: "DailyObservation, StationMeta, WindowSpec, ComponentScores, CombinedScore contracts"
      contains: "export interface DailyObservation"
    - path: "packages/domain/src/index.ts"
      provides: "public barrel re-exporting every domain module"
      contains: "export"
    - path: "scripts/skeleton-demo.ts"
      provides: "end-to-end CLI: fetch real data -> domain -> printed result"
      min_lines: 20
    - path: "test/e2e/skeleton.test.ts"
      provides: "failing-first end-to-end test for the happy path chain"
      contains: "skeleton"
  key_links:
    - from: "scripts/skeleton-demo.ts"
      to: "packages/fetch/src/observations.ts"
      via: "import fetchAwsDay"
      pattern: "from ['\"].*fetch"
    - from: "scripts/skeleton-demo.ts"
      to: "packages/domain/src/index.ts"
      via: "import domain functions"
      pattern: "from ['\"]@betravedur/domain"
---

<objective>
Build the Walking Skeleton for Betra Veður: the thinnest real end-to-end slice within Phase 1's boundary (data access + domain math). Stand up an npm-workspaces TypeScript monorepo with Vitest, create the pure `@betravedur/domain` package with fully-typed contracts and stub implementations of every domain function, create a thin `@betravedur/fetch` client that hits the live api.vedur.is, and wire a single CLI (`scripts/skeleton-demo.ts`) that fetches real daily observations for 2-3 stations and runs them through the domain layer to print a human-readable result. This proves the whole chain works on real data before Plans 02-04 fill in correct math, registry, and score.

Purpose: De-risk the architecture (shared pure-TS domain, Node fetch client, workspace wiring) with a working skeleton, and hand Plans 02/03/04 a stable contract surface (types + module signatures) so they implement against fixed interfaces rather than exploring.
Output: Working monorepo, contract types, stubbed domain modules, live fetch client, and a runnable end-to-end demo CLI proving real data flows through the domain package.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-access-domain-core/01-CONTEXT.md
@.planning/phases/01-data-access-domain-core/01-RESEARCH.md
@.planning/phases/01-data-access-domain-core/SKELETON.md

<interfaces>
<!-- Live-verified API response shapes (api.vedur.is, 2026-07-19) the skeleton must parse. -->
<!-- AWS day row: dv present (wind dir), r=null (NO precip on AWS). -->
<!-- {"station":1350,"name":"Keflavíkurflugvöllur","time":"2024-07-15",
      "t":11.97,"tx":14.6,"tn":10.2,"f":5.0,"fx":7.7,"fg":10.5,
      "dv":151.0,"dv_txt":"SSE","r":null,"rsun":null,"count_measurements":24} -->
<!-- SYNOP day row: r present (precip), sun+n present, NO dv (wind direction). -->
<!-- {"station":1,"name":"Reykjavík","time":"2024-09-01","t":...,"txx":17.4,"tnn":10.8,
      "f":2.6,"fx":5.8,"fg":8.6,"sun":11.0,"n":5.4,"r":15.2,"r_type":6} -->
<!-- Base URL: https://api.vedur.is/weather. Multi-station: repeat station_id param.
      Date range: day_from/day_to = YYYY-MM-DD (together). parameters=basic|all. format=json.
      Errors: 404 {"message":"No data found."} / {"message":"Station/s not found."}; 422 {"detail":[...]}. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold npm-workspaces TS monorepo with Vitest and a passing smoke test</name>
  <files>package.json, package-lock.json, tsconfig.base.json, vitest.config.ts, .gitignore, .nvmrc, packages/domain/package.json, packages/domain/tsconfig.json, packages/domain/src/index.ts, packages/domain/test/smoke.test.ts, packages/fetch/package.json, packages/fetch/tsconfig.json</files>
  <read_first>
    - /Users/jonb/Projects/betravedur/.planning/phases/01-data-access-domain-core/01-RESEARCH.md (Standard Stack table + Recommended Project Structure — exact versions and layout)
    - /Users/jonb/Projects/betravedur/CLAUDE.md (project constraints: static site, Icelandic, GitHub Pages)
    - /Users/jonb/Projects/betravedur/.planning/phases/01-data-access-domain-core/SKELETON.md (architectural decisions this scaffold must honor)
  </read_first>
  <action>
    Create the monorepo root `package.json` with `"type": "module"`, `"private": true`, `"workspaces": ["packages/*"]`, and a `"test": "vitest run"` script. Do NOT set `"engines"` below Node 20.19.
    Install dev tools pinned to the researched versions: `typescript@7` (7.0.2), `vitest@4` (4.1.10), `@vitest/coverage-v8@4` (4.1.10), `tsx@4` (4.23.1). These are the four ASSUMED packages gated by the Task 2 legitimacy checkpoint in Plan 03's threat model context — installation here is acceptable because all four are first-party reference tooling with empty postinstall (verified in RESEARCH.md Package Legitimacy Audit); if `npm install` reports any postinstall script for these four, STOP and surface it.
    Create `tsconfig.base.json` with `strict: true`, `module: "nodenext"`, `moduleResolution: "nodenext"`, `target: "es2023"`, `lib: ["es2023"]`, `declaration: true`, `noUncheckedIndexedAccess: true`, `verbatimModuleSyntax: true`. The domain package must be browser-safe: its tsconfig must NOT include `"types": ["node"]` and must NOT reference Node built-in libs.
    Create `vitest.config.ts` at root configuring `test.include` for `packages/**/test/**/*.test.ts` and `test/**/*.test.ts`, and V8 coverage for `packages/domain/src`. Do NOT enable watch mode in any script.
    Create `packages/domain/package.json` (name `@betravedur/domain`, `"type": "module"`, `main`/`exports` pointing at `src/index.ts`, ZERO `dependencies`), and `packages/domain/tsconfig.json` extending the base with browser-safe libs only.
    Create `packages/fetch/package.json` (name `@betravedur/fetch`, `"type": "module"`, `dependencies`: `{"@betravedur/domain": "*"}` via workspace, no other runtime deps — uses native `fetch`), and `packages/fetch/tsconfig.json` extending base with `"types": ["node"]`.
    Create `packages/domain/src/index.ts` as a minimal barrel that re-exports from `./types` (create `./types` in Task 2 — for this task, index.ts may temporarily export only a `DOMAIN_VERSION = "0.1.0"` const so the smoke test has something to import).
    Create `packages/domain/test/smoke.test.ts` asserting the imported `DOMAIN_VERSION` equals `"0.1.0"`.
    Create `.nvmrc` with `25` and `.gitignore` covering `node_modules`, `dist`, `coverage`, `*.log`.
  </action>
  <verify>
    <automated>cd /Users/jonb/Projects/betravedur && npm install --no-audit --no-fund >/dev/null 2>&1 && npx vitest run packages/domain/test/smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm install` completes with exit 0 and creates `node_modules` + `package-lock.json`
    - `npx vitest run packages/domain/test/smoke.test.ts` reports 1 passing test, 0 failing
    - `node -e "import('./packages/domain/src/index.ts')"` style import does not require any runtime dependency (grep `packages/domain/package.json` shows no `"dependencies"` block or an empty one)
    - `package.json` contains the literal string `"workspaces"` and `"type": "module"`
    - `npm ls typescript vitest tsx 2>/dev/null` shows typescript@7.x, vitest@4.x, tsx@4.x
  </acceptance_criteria>
  <done>Fresh clone runs `npm install && npx vitest run` green; workspace resolves both packages; domain package has zero runtime deps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define domain contracts + stub every domain module (interface-first)</name>
  <files>packages/domain/src/types.ts, packages/domain/src/index.ts, packages/domain/src/window.ts, packages/domain/src/coverage.ts, packages/domain/src/wind.ts, packages/domain/src/precip.ts, packages/domain/src/score.ts, packages/domain/src/attribution.ts</files>
  <read_first>
    - /Users/jonb/Projects/betravedur/.planning/phases/01-data-access-domain-core/01-RESEARCH.md (Recommended Project Structure module list; Patterns 1-3 for signatures; Field cheat-sheet for observation fields)
    - /Users/jonb/Projects/betravedur/.planning/phases/01-data-access-domain-core/01-CONTEXT.md (locked decisions: renormalized score, contributing components, "án úrkomu" flag, N>=3, >=80% coverage, Feb-29 fold)
    - /Users/jonb/Projects/betravedur/packages/domain/src/index.ts (current barrel from Task 1)
  </read_first>
  <behavior>
    - types.ts compiles under strict mode and is importable with zero runtime deps
    - Every stub function is exported with its final signature; stubs throw `new Error("NOT_IMPLEMENTED")` (implementations land in Plans 02 and 04)
    - The barrel `index.ts` re-exports every module so downstream imports (`@betravedur/domain`) resolve all names
  </behavior>
  <action>
    Create `packages/domain/src/types.ts` with these exported contracts (concrete field names from the live API cheat-sheet):
      - `DailyObservation`: `{ station: number; date: string; doy: number; t: number|null; tx: number|null; tn: number|null; f: number|null; fx: number|null; fg: number|null; dv: number|null; r: number|null }` where `doy` is the leap-folded day-of-year (1-365, Feb 29 excluded).
      - `StationMeta`: `{ station: number; name: string; type: 'sj'|'sk'|'ur'|'vf'; owner: string; lat: number; lon: number; ele: number; start: number; ending: number|null }`.
      - `WindowSpec`: `{ startDoy: number; endDoy: number }` (inclusive, may wrap year-end).
      - `Component`: `'temp'|'rain'|'wind'`.
      - `ComponentScores`: `{ temp: number|null; rain: number|null; wind: number|null }` (each 0-10 or null when unavailable).
      - `CombinedScore`: `{ score: number; contributing: Component[]; missingRain: boolean }` — `score` 0-10 renormalized over `contributing`; `missingRain` drives the "án úrkomu" badge.
      - `WindowAverage`: `{ metric: string; value: number|null; n: number; sufficient: boolean }` where `sufficient` is `n >= 3`.
    Create stub modules with FINAL signatures, each body `throw new Error("NOT_IMPLEMENTED")`:
      - `window.ts`: `export function leapFoldedDoy(date: string): number|null` (returns null for Feb 29); `export function expandWindow(spec: WindowSpec): Set<number>`.
      - `coverage.ts`: `export function qualifyingYears(rowsByYear: Map<number, DailyObservation[]>, windowDays: Set<number>, metric: (o: DailyObservation) => number|null, minCoverage?: number): number[]`; `export function effectiveN(qualifying: number[]): { n: number; sufficient: boolean }`.
      - `wind.ts`: `export function circularMeanDirection(samples: { speed: number; dirDeg: number }[]): { dirDeg: number; resultantSpeed: number }|null`; `export function scalarMeanSpeed(speeds: (number|null)[]): number|null`.
      - `precip.ts`: `export function sumPerYearThenAverage(rowsByYear: Map<number, DailyObservation[]>, windowDays: Set<number>, qualifying: number[]): number|null`.
      - `score.ts`: `export function tempComponent(meanTempC: number): number`; `export function rainComponent(typicalTotalMm: number): number`; `export function windComponent(meanSpeedMs: number): number`; `export function combine(components: ComponentScores, weights?: { temp: number; rain: number; wind: number }): CombinedScore`.
      - `attribution.ts`: `export const ATTRIBUTION: { license: string; text_is: string; text_en: string; sourceUrl: string; modifiedNotice_is: string }` stubbed with empty strings (Plan 03 fills the verified CC BY 4.0 wording).
    Rewrite `packages/domain/src/index.ts` to `export * from './types'` and `export * from` each module above. Remove the temporary `DOMAIN_VERSION` const OR keep it and update the smoke test accordingly (your discretion — keep the smoke test green).
  </action>
  <verify>
    <automated>cd /Users/jonb/Projects/betravedur && npx tsc -p packages/domain/tsconfig.json --noEmit && npx vitest run packages/domain/test/smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -p packages/domain/tsconfig.json --noEmit` exits 0 (types compile under strict + browser-safe libs)
    - `grep -c "export" packages/domain/src/types.ts` returns >= 7 (all contracts exported)
    - `grep -rn "NOT_IMPLEMENTED" packages/domain/src | grep -v '^#' | grep -c NOT_IMPLEMENTED` returns >= 9 (every math/score stub present)
    - `grep -q "circularMeanDirection" packages/domain/src/index.ts` succeeds (barrel re-exports)
    - smoke test still passes
  </acceptance_criteria>
  <done>All domain contracts + stubs exist with final signatures, compile browser-safe, and are re-exported from the barrel. Plans 02/04 can implement against fixed interfaces.</done>
</task>

<task type="auto">
  <name>Task 3: Live fetch client + end-to-end demo CLI proving the full chain on real data</name>
  <files>packages/fetch/src/client.ts, packages/fetch/src/observations.ts, packages/fetch/src/stations.ts, scripts/skeleton-demo.ts, test/e2e/skeleton.test.ts</files>
  <read_first>
    - /Users/jonb/Projects/betravedur/.planning/phases/01-data-access-domain-core/01-RESEARCH.md (Code Examples: fetchAwsDay + fetchWithRetry; API Reference table; verified response shapes)
    - /Users/jonb/Projects/betravedur/packages/domain/src/types.ts (DailyObservation shape to normalize into)
    - /Users/jonb/Projects/betravedur/packages/domain/src/index.ts (domain functions the demo will call)
  </read_first>
  <action>
    Create `packages/fetch/src/client.ts` with `fetchWithRetry(url: string, tries = 3): Promise<Response>` exactly per the RESEARCH Code Example: return immediately on `r.ok`; do NOT retry deterministic `404`/`422`; exponential backoff `500 * 2**i` on 5xx/network; throw after `tries`. Set `Accept: application/json`. Base URL constant `https://api.vedur.is/weather`.
    Create `packages/fetch/src/observations.ts` with `fetchAwsDay(stationIds: number[], from: string, to: string): Promise<DailyObservation[]>` and `fetchSynopDay(stationIds: number[], from: string, to: string): Promise<DailyObservation[]>`. Build the query with `URLSearchParams`, repeating `station_id` per id, `day_from`/`day_to`, `parameters=basic`, `format=json`. Normalize each raw row into `DailyObservation` (map `time`->`date`, compute `doy` via `leapFoldedDoy`, keep `r: null` for AWS, keep `dv: null` for SYNOP). This is a thin skeleton normalizer — full schema-assert/clamping lands in Plan 03.
    Create `packages/fetch/src/stations.ts` with `fetchStations(stationIds: number[]): Promise<StationMeta[]>` hitting `/stations` and mapping to `StationMeta`.
    Create `scripts/skeleton-demo.ts` (runnable via `tsx`): fetch a fixed baseline window (e.g. `2024-07-15`..`2024-07-25`) of AWS observations for stations `[1350, 1395, 1470]` (Keflavík/Eyrarbakki/Reykjavík AWS — verified live), and SYNOP observations for station `[1]` (Reykjavík), run each station's rows through `expandWindow` + `circularMeanDirection` + `scalarMeanSpeed`, and print a per-station human-readable line to stdout (station name, mean wind dir if available, mean speed, precip-availability note). Because Plan 02 has not implemented the math yet, the demo MUST tolerate the `NOT_IMPLEMENTED` stubs by catching and printing `"[domain math pending Plan 02]"` for those fields — the point of THIS task is proving real data reaches the domain boundary, not computing final numbers. Print the fetched raw field presence (dv present on AWS, r present on SYNOP) as proof the live chain works.
    Create `test/e2e/skeleton.test.ts`: an end-to-end happy-path test that runs the fetch layer against the live API (skipped automatically via `it.skipIf(!process.env.BETRA_LIVE)` when offline) AND an always-on offline assertion that `scripts/skeleton-demo.ts`'s importable functions exist and that `fetchAwsDay`/`fetchSynopDay` are exported. This is the failing-first test that becomes green once the chain is wired.
  </action>
  <verify>
    <automated>cd /Users/jonb/Projects/betravedur && npx tsc -p packages/fetch/tsconfig.json --noEmit && npx vitest run test/e2e/skeleton.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -p packages/fetch/tsconfig.json --noEmit` exits 0
    - `grep -q "500 \* 2 \*\* i" packages/fetch/src/client.ts` OR `grep -Eq "500 ?\* ?2" packages/fetch/src/client.ts` succeeds (backoff present)
    - `grep -Eq "404|422" packages/fetch/src/client.ts` succeeds (deterministic errors not retried)
    - `npx vitest run test/e2e/skeleton.test.ts` passes the always-on offline assertions
    - `grep -q "fetchAwsDay" scripts/skeleton-demo.ts && grep -q "@betravedur/domain" scripts/skeleton-demo.ts` succeeds (CLI wires fetch -> domain)
  </acceptance_criteria>
  <done>Fetch client compiles; demo CLI imports both packages and is structured to fetch real data and route it through the domain boundary; e2e test green offline. Real-data run is verified in Task 4 checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The Walking Skeleton end-to-end chain: npm-workspaces monorepo, pure `@betravedur/domain` with typed contracts + stubs, live `@betravedur/fetch` client, and `scripts/skeleton-demo.ts` that fetches real daily observations from api.vedur.is for 2-3 AWS stations + 1 SYNOP station and routes them through the domain boundary.</what-built>
  <how-to-verify>
    1. From the repo root, run the live demo:
       `BETRA_LIVE=1 npx tsx scripts/skeleton-demo.ts`
    2. Confirm the output shows a line per station with:
       - a real station name (e.g. "Keflavíkurflugvöllur")
       - for the AWS stations: wind direction present (`dv` non-null) and a "vantar úrkomu / án úrkomu" style note (r is null on AWS — this is the expected structural finding)
       - for the SYNOP station (Reykjavík, id 1): precipitation present (`r` non-null) and a wind-direction-absent note
       - domain math fields may print "[domain math pending Plan 02]" — that is expected at this stage
    3. Confirm no unhandled exception / non-zero exit.
    4. Optional: re-run to confirm retry/backoff does not spam on success.
    This proves the full data->domain chain works on REAL Veðurstofan data before Plans 02-04 fill in correct math.
  </how-to-verify>
  <resume-signal>Type "approved" if real per-station output appears with AWS=dv-present/rain-absent and SYNOP=rain-present, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| api.vedur.is -> fetch package | Untrusted upstream JSON crosses into the build-time client; may drift or return malformed rows |
| npm registry -> dev toolchain | Third-party packages installed into the build environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | api.vedur.is response consumed by observations.ts | accept (skeleton) / mitigate in Plan 03 | Skeleton normalizes minimally; full schema-assert (`x-vi-api-version` + expected field set) and value clamping are Plan 03's DATA-01 hardening. Documented handoff, not a silent gap. |
| T-01-02 | Tampering | npm dev dependencies (typescript, vitest, coverage-v8, tsx) | mitigate | Versions pinned to RESEARCH-verified releases; all four have empty postinstall (verified). Task 1 aborts if any postinstall appears. Legitimacy checkpoint formally lives in Plan 03 threat model per shared audit. |
| T-01-03 | Denial of Service | fetchWithRetry backoff loop | mitigate | Bounded retries (3), no retry on deterministic 404/422, exponential backoff — cannot hot-loop against the upstream. |
| T-01-04 | Information disclosure | committed repo | mitigate | Keyless public API — no secrets to leak; `.gitignore` excludes node_modules; assert no credential files created. |
| T-01-SC | Tampering | npm installs (typescript/vitest/coverage-v8/tsx) | mitigate | slopcheck unavailable in env -> packages marked [ASSUMED]; all first-party reference tooling, empty postinstall. Blocking legitimacy checkpoint carried in Plan 03. |
</threat_model>

<verification>
- `npm install && npx vitest run` green from clean state.
- Domain package has zero runtime dependencies (`packages/domain/package.json` dependencies empty/absent).
- Domain package compiles browser-safe (no Node libs in its tsconfig).
- `BETRA_LIVE=1 npx tsx scripts/skeleton-demo.ts` prints real per-station output (human checkpoint).
</verification>

<success_criteria>
- Monorepo scaffolded, both workspaces resolve, smoke test green.
- All domain contracts + stub signatures exist and are re-exported from the barrel.
- Live fetch client + demo CLI route real Veðurstofan data through the domain boundary.
- Human confirms real-data output shows the AWS(dv-present/rain-absent) vs SYNOP(rain-present) structural split.
</success_criteria>

<output>
Create `.planning/phases/01-data-access-domain-core/01-01-SUMMARY.md` when done.
</output>
