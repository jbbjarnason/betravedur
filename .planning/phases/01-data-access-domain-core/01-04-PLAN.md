---
phase: 01-data-access-domain-core
plan: 04
type: tdd
wave: 3
depends_on: [01-02, 01-03]
files_modified:
  - packages/domain/src/score.ts
  - packages/domain/test/score.test.ts
  - scripts/skeleton-demo.ts
  - test/e2e/skeleton.test.ts
autonomous: true
requirements: [SCORE-01]
user_setup: []

must_haves:
  truths:
    - "Each component (temp/rain/wind) maps to a 0-10 score via a fixed, explainable piecewise-linear curve: temp best toward ~15-20C, rain and wind less-is-better"
    - "combine() renormalizes the rain 0.4 / wind 0.3 / temp 0.3 weights over ONLY the available components so a station missing rain is scored fairly on wind+temp"
    - "The combined score records which components contributed and sets a missingRain flag so the UI can show the 'án úrkomu' badge"
    - "A SYNOP station with all three components gets the full three-component score; an AWS station without rain gets a renormalized two-component score"
    - "The end-to-end demo prints a per-station combined score computed from REAL fetched data through the full chain"
  artifacts:
    - path: "packages/domain/src/score.ts"
      provides: "tempComponent/rainComponent/windComponent curves + renormalizing combine()"
      contains: "contributing"
    - path: "packages/domain/test/score.test.ts"
      provides: "component-curve + renormalization + missing-component tests"
      contains: "renormal"
    - path: "scripts/skeleton-demo.ts"
      provides: "full-chain demo now emits real combined scores"
      contains: "combine"
  key_links:
    - from: "scripts/skeleton-demo.ts"
      to: "packages/domain/src/score.ts"
      via: "combine(components) called on real aggregated data"
      pattern: "combine\\("
    - from: "packages/domain/src/score.ts"
      to: "packages/domain/src/types.ts"
      via: "returns CombinedScore { score, contributing, missingRain }"
      pattern: "CombinedScore"
---

<objective>
Implement the combined weather score — the payoff of the phase — as fixed, explainable 0-10 component curves (temp/rain/wind) plus a `combine()` that renormalizes the default rain 0.4 / wind 0.3 / temp 0.3 weights over only the components a station actually has, records the contributing components, and flags stations scored without rain ("án úrkomu"). Then close the Walking Skeleton loop: upgrade `scripts/skeleton-demo.ts` so that, using the now-real math from Plan 02, the real fetched data from Plan 03, it computes and prints a genuine per-station combined score end-to-end, and the e2e test asserts the full chain produces a score. This directly implements the CONTEXT decision upgraded after the AWS-no-precip finding.

Purpose: SCORE-01 — components computed separately (so weight sliders stay possible, WGT-01) and combined at display time with honest renormalization for the structural reality that most stations lack precipitation. Without this the map could not answer "where has it been best."
Output: Implemented + tested score module and a working end-to-end demo emitting real combined scores.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-data-access-domain-core/01-CONTEXT.md
@.planning/phases/01-data-access-domain-core/01-RESEARCH.md

<interfaces>
<!-- Signatures FIXED by Plan 01. Implement, do not redesign. -->
<!-- score.ts: tempComponent(meanTempC:number):number       (0-10, best ~15-20C)
              rainComponent(typicalTotalMm:number):number   (0-10, less-is-better)
              windComponent(meanSpeedMs:number):number      (0-10, less-is-better)
              combine(components: ComponentScores, weights?): CombinedScore -->
<!-- ComponentScores { temp:number|null; rain:number|null; wind:number|null } -->
<!-- CombinedScore  { score:number; contributing:Component[]; missingRain:boolean } -->
<!-- Default weights (CONTEXT): rain 0.4 / wind 0.3 / temp 0.3, renormalized over available components. -->
<!-- Plan 02 provides: circularMeanDirection, scalarMeanSpeed, sumPerYearThenAverage, qualifyingYears, expandWindow, leapFoldedDoy. -->
<!-- Plan 03 provides: fetchAwsDay, fetchSynopDay (normalized DailyObservation[]). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fixed 0-10 component curves + renormalizing combine() with contributing-components record</name>
  <files>packages/domain/src/score.ts, packages/domain/test/score.test.ts</files>
  <read_first>
    - /Users/jonb/Projects/betravedur/.planning/phases/01-data-access-domain-core/01-CONTEXT.md (Combined Weather Score: 0-10 curves, weights rain 0.4/wind 0.3/temp 0.3, renormalization over available components, "án úrkomu", record contributing components)
    - /Users/jonb/Projects/betravedur/.planning/phases/01-data-access-domain-core/01-RESEARCH.md (Pitfall 1 AWS-no-precip; SCORE-01 component-separation design; Open Question 1 on per-component scoring)
    - /Users/jonb/Projects/betravedur/packages/domain/src/score.ts (stub signatures)
    - /Users/jonb/Projects/betravedur/packages/domain/src/types.ts (ComponentScores, CombinedScore, Component)
  </read_first>
  <behavior>
    - tempComponent: peaks near 10 at ~15-20C, decreasing toward 0 at cold/hot extremes; monotone-decreasing on each side of the peak; output clamped to [0,10]
    - rainComponent: 10 at 0mm typical total, decreasing as typical rain increases; clamped [0,10] (less-is-better)
    - windComponent: 10 at 0 m/s mean, decreasing as wind increases; clamped [0,10] (less-is-better)
    - combine with all three present uses weights rain 0.4/wind 0.3/temp 0.3; result.contributing = ['temp','rain','wind'] (order not asserted); missingRain === false
    - combine with rain === null renormalizes to wind 0.5 / temp 0.5 (0.3/0.6 and 0.3/0.6); contributing excludes 'rain'; missingRain === true
    - combine where a two-component score equals the correctly renormalized weighted average (assert the exact number for a chosen input, e.g. wind=6 temp=8 -> 7.0)
    - combine with only one component present returns that component's score directly (weight renormalizes to 1)
    - combine with all components null returns score handled as null-or-0 with contributing=[] (choose one; test it explicitly)
  </behavior>
  <action>
    Implement the three component functions as piecewise-linear curves (Claude's discretion on exact breakpoints, per CONTEXT — keep them simple and explainable for the SCORE-03 panel; document each breakpoint in a comment). Suggested, explainable shapes: tempComponent flat-ish 10 across ~13-20C, linearly down to 0 by ~-5C and by ~30C; rainComponent 10 at 0mm down to 0 by some window-total threshold (e.g. ~60mm typical total); windComponent 10 at 0 m/s down to 0 by ~15 m/s mean. Clamp all outputs to [0,10].
    Implement `combine(components, weights = { rain: 0.4, wind: 0.3, temp: 0.3 })`: collect the non-null components; if none, return the agreed empty result (`{ score: 0, contributing: [], missingRain: true }` OR a null score — pick and test it); otherwise renormalize the weights of present components to sum to 1, compute the weighted average, set `contributing` to the present component names, and set `missingRain = !contributing.includes('rain')`. Round `score` sensibly (e.g. one decimal) but keep it a number.
    Write `score.test.ts` covering every behavior bullet; include a test named `"component score"` (component curves + full combine) and `"missing component excluded"`/`"renormalize"` (the AWS-no-rain renormalization + missingRain flag), matching the RESEARCH test-map selectors. Assert the exact renormalized number for wind=6,temp=8 (expect 7.0) so the math is pinned, not just "runs".
  </action>
  <verify>
    <automated>cd /Users/jonb/Projects/betravedur && npx vitest run packages/domain/test/score.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run packages/domain -t "component score"` passes
    - `npx vitest run packages/domain -t "renormal"` OR `-t "missing component excluded"` passes (renormalization + missingRain)
    - a test pins the exact renormalized value (wind=6,temp=8 -> 7.0) and passes
    - `grep -q "contributing" packages/domain/src/score.ts` and `grep -q "missingRain" packages/domain/src/score.ts` succeed
    - `grep -q "NOT_IMPLEMENTED" packages/domain/src/score.ts; test $? -ne 0` (stub filled)
    - full domain suite green: `npx vitest run packages/domain` exits 0
  </acceptance_criteria>
  <done>Component curves + renormalizing combine implemented and tested; missing-rain handled via renormalization + "án úrkomu" flag + contributing record; SCORE-01 satisfied at the math level.</done>
</task>

<task type="auto">
  <name>Task 2: Close the Walking Skeleton loop — real combined scores end-to-end</name>
  <files>scripts/skeleton-demo.ts, test/e2e/skeleton.test.ts</files>
  <read_first>
    - /Users/jonb/Projects/betravedur/scripts/skeleton-demo.ts (Plan 01 skeleton demo to upgrade)
    - /Users/jonb/Projects/betravedur/test/e2e/skeleton.test.ts (Plan 01 e2e test to strengthen)
    - /Users/jonb/Projects/betravedur/packages/domain/src/index.ts (now-real domain exports)
    - /Users/jonb/Projects/betravedur/packages/fetch/src/observations.ts (real fetchers from Plan 03)
    - /Users/jonb/Projects/betravedur/.planning/phases/01-data-access-domain-core/01-CONTEXT.md ("án úrkomu" badge, N>=3 gate, ófullnægjandi gögn)
  </read_first>
  <action>
    Upgrade `scripts/skeleton-demo.ts` to run the complete real chain (no more "[domain math pending]" placeholders): for each demo station, fetch a baseline window across several years (e.g. `2011-07-15`..`2011-07-25` through `2015-...` — a few years so N>=3 can be reached), build `rowsByYear`, `expandWindow` the July window, compute `qualifyingYears` + `effectiveN` per metric, and where `sufficient` compute meanTemp (scalar), scalarMeanSpeed, circularMeanDirection, and sumPerYearThenAverage; map those to `tempComponent`/`windComponent`/`rainComponent` (rain only for SYNOP), then `combine()`. Print per station: name, N, mean temp, mean wind dir+speed, typical rain (or "án úrkomu" for AWS), and the combined score with its contributing components. Where N<3 print "ófullnægjandi gögn". Keep the offline/`BETRA_LIVE` guard from Plan 01 for the live-fetch path.
    Strengthen `test/e2e/skeleton.test.ts`: add an OFFLINE integration test that feeds a small hand-built `DailyObservation[]` (spanning >=3 years with >=80% window coverage) through the exact same chain functions (`expandWindow` -> `qualifyingYears` -> means -> components -> `combine`) and asserts a numeric `CombinedScore` with correct `contributing`/`missingRain` comes out — no network needed. This makes the full-chain assertion deterministic in CI. Keep the live path behind `it.skipIf(!process.env.BETRA_LIVE)`.
  </action>
  <verify>
    <automated>cd /Users/jonb/Projects/betravedur && npx vitest run test/e2e/skeleton.test.ts && npx tsc -p packages/fetch/tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/e2e/skeleton.test.ts` passes including the offline full-chain integration test that asserts a numeric combined score
    - `grep -q "combine(" scripts/skeleton-demo.ts` succeeds (score wired into the demo)
    - `grep -Eq "án úrkomu|missingRain" scripts/skeleton-demo.ts` succeeds (missing-rain surfaced)
    - `grep -Eq "ófullnægjandi|sufficient" scripts/skeleton-demo.ts` succeeds (N<3 gate surfaced)
    - no `\[domain math pending` placeholder remains: `grep -q "pending Plan 02" scripts/skeleton-demo.ts; test $? -ne 0`
    - full suite green: `npx vitest run` exits 0
  </acceptance_criteria>
  <done>The Walking Skeleton computes real combined scores end-to-end from real Veðurstofan data; offline e2e test pins the full chain; the phase's core capability is proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| aggregated components -> score combine | Missing/partial components must not produce a misleading score |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13 | Tampering | score.ts missing-component handling | mitigate | combine() renormalizes over present components and flags missingRain; an AWS station without rain cannot silently score as dry paradise (Pitfall 1) — contributing record + "án úrkomu" badge keep the score honest. |
| T-01-14 | Repudiation | score transparency | mitigate | Fixed, documented piecewise-linear curves + recorded contributing components make the score explainable (SCORE-03 groundwork); no hidden weighting. |
| T-01-15 | Tampering | supply chain (domain pkg) | mitigate | Score math stays in the dependency-free domain package; no new runtime deps introduced. |
</threat_model>

<verification>
- `npx vitest run` full suite green.
- Named score tests pass: "component score", renormalization/"missing component excluded".
- Exact renormalized value pinned (wind=6,temp=8 -> 7.0).
- Offline e2e full-chain test asserts a real CombinedScore.
- `BETRA_LIVE=1 npx tsx scripts/skeleton-demo.ts` prints real per-station combined scores.
</verification>

<success_criteria>
- Combined score from separately-computed temp/rain/wind components with swappable weights (SCORE-01).
- Renormalization over available components + "án úrkomu" flag + contributing record (CONTEXT decision).
- Walking Skeleton closes: real data -> domain -> real combined score, end-to-end.
</success_criteria>

<output>
Create `.planning/phases/01-data-access-domain-core/01-04-SUMMARY.md` when done.
</output>
