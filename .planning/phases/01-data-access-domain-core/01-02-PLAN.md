---
phase: 01-data-access-domain-core
plan: 02
type: tdd
wave: 2
depends_on: [01-01]
files_modified:
  - packages/domain/src/window.ts
  - packages/domain/src/coverage.ts
  - packages/domain/src/wind.ts
  - packages/domain/src/precip.ts
  - packages/domain/test/window.test.ts
  - packages/domain/test/coverage.test.ts
  - packages/domain/test/wind.test.ts
  - packages/domain/test/precip.test.ts
autonomous: true
requirements: [DATA-05]
user_setup: []

must_haves:
  truths:
    - "Circular mean of wind directions 350 and 10 degrees returns approximately 0 (north), never 180"
    - "Wind speed is averaged as a scalar, separately from direction"
    - "A year counts toward N only if >=80% of the leap-folded window days have a usable observation for the metric; N<3 yields sufficient:false"
    - "Precipitation is summed per year over present days then averaged across qualifying years; a missing day is skipped, never coerced to zero"
    - "Feb 29 is folded out so July 19 maps to the same day-of-year index in leap and non-leap years"
  artifacts:
    - path: "packages/domain/src/wind.ts"
      provides: "circularMeanDirection (u/v vector mean) + scalarMeanSpeed"
      contains: "atan2"
    - path: "packages/domain/src/coverage.ts"
      provides: "qualifyingYears (>=80%) + effectiveN (N>=3 gate)"
      contains: "minCoverage"
    - path: "packages/domain/src/precip.ts"
      provides: "sumPerYearThenAverage, missing != zero"
      min_lines: 15
    - path: "packages/domain/src/window.ts"
      provides: "leapFoldedDoy (Feb-29 -> null) + expandWindow (wrap-around)"
      contains: "leapFoldedDoy"
    - path: "packages/domain/test/wind.test.ts"
      provides: "named 350/10 circular-mean test"
      contains: "350"
  key_links:
    - from: "packages/domain/src/coverage.ts"
      to: "packages/domain/src/window.ts"
      via: "window day-of-year set drives the coverage denominator"
      pattern: "windowDays"
---

<objective>
Implement the coverage-honest climatology math that is the correctness heart of the whole product: leap-folded day-of-year window selection, unit-vector circular mean for wind direction (the named 350/10 case), scalar wind-speed mean, coverage-honest qualifying-years / effective-N, and precipitation sum-per-year-then-average with missing treated as missing. Every function replaces a `NOT_IMPLEMENTED` stub from Plan 01 against the fixed signatures already exported. This plan is TDD: tests encode the CONTEXT-locked rules first, then implementations turn them green.

Purpose: These four modules are imported verbatim by both the Phase 2 pipeline and the Phase 3+ browser client. A wrong circular mean or a null-coerced-to-zero precip silently corrupts every average the product ever shows. Correctness here is the phase's core promise ("meðaltal N ára" must be honest).
Output: Fully-implemented, fully-tested window/coverage/wind/precip domain modules; the named 350/10 test passes; DATA-05 satisfied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-data-access-domain-core/01-CONTEXT.md
@.planning/phases/01-data-access-domain-core/01-RESEARCH.md

<interfaces>
<!-- Signatures are FIXED by Plan 01 (packages/domain/src/types.ts + module stubs). Implement, do not redesign. -->
<!-- window.ts:   leapFoldedDoy(date: string): number|null   (Feb 29 -> null)
                  expandWindow(spec: WindowSpec): Set<number> (inclusive, may wrap year-end) -->
<!-- coverage.ts: qualifyingYears(rowsByYear, windowDays, metric, minCoverage=0.8): number[]
                  effectiveN(qualifying): { n: number; sufficient: boolean }  (sufficient = n>=3) -->
<!-- wind.ts:     circularMeanDirection(samples: {speed,dirDeg}[]): {dirDeg,resultantSpeed}|null
                  scalarMeanSpeed(speeds: (number|null)[]): number|null -->
<!-- precip.ts:   sumPerYearThenAverage(rowsByYear, windowDays, qualifying): number|null -->
<!-- DailyObservation carries a precomputed leap-folded `doy` field (set by the fetch normalizer). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Window selection + coverage-honest N (leap-fold, >=80%, N>=3)</name>
  <files>packages/domain/src/window.ts, packages/domain/src/coverage.ts, packages/domain/test/window.test.ts, packages/domain/test/coverage.test.ts</files>
  <read_first>
    - /Users/jonb/Projects/betravedur/.planning/phases/01-data-access-domain-core/01-CONTEXT.md (Climatology Windows & N-honesty: >=80%, N>=3, Feb-29 excluded)
    - /Users/jonb/Projects/betravedur/.planning/phases/01-data-access-domain-core/01-RESEARCH.md (Pattern 2 coverage code; Pitfall 5 leap-day fold)
    - /Users/jonb/Projects/betravedur/packages/domain/src/window.ts (stub signatures)
    - /Users/jonb/Projects/betravedur/packages/domain/src/coverage.ts (stub signatures)
    - /Users/jonb/Projects/betravedur/packages/domain/src/types.ts (DailyObservation, WindowSpec, WindowAverage)
  </read_first>
  <behavior>
    - leapFoldedDoy("2024-07-19") === leapFoldedDoy("2023-07-19") (leap and non-leap map July 19 to the SAME index)
    - leapFoldedDoy("2024-02-29") === null (Feb 29 folded out)
    - leapFoldedDoy("2024-03-01") === leapFoldedDoy("2023-03-01") (March 1 index unaffected by the leap day before it)
    - expandWindow({startDoy: 360, endDoy: 5}) wraps year-end and yields a set spanning 360..365 and 1..5
    - qualifyingYears: a year with 6 of 7 window days present (0.857 >= 0.8) qualifies; a year with 5 of 7 (0.714 < 0.8) does not
    - qualifyingYears counts a day only if metric(o) != null (a present row with null metric does not count)
    - effectiveN([2011,2012]) -> {n:2, sufficient:false}; effectiveN([2011,2012,2013]) -> {n:3, sufficient:true}
  </behavior>
  <action>
    Implement `leapFoldedDoy(date)` in `window.ts`: parse the ISO `YYYY-MM-DD`; if month=2 & day=29 return `null`; otherwise compute a day-of-year that ignores Feb 29 entirely (use a fixed non-leap-year day table or subtract one for post-Feb-29 dates in leap years) so the same calendar date yields an identical integer in every year. Range 1..365. Do NOT import any date library — plain arithmetic per RESEARCH "Don't Hand-Roll".
    Implement `expandWindow(spec)`: return a `Set<number>` of leap-folded doy indices from `startDoy` to `endDoy` inclusive, wrapping past 365 back to 1 when `endDoy < startDoy`.
    Implement `qualifyingYears(rowsByYear, windowDays, metric, minCoverage=0.8)` per RESEARCH Pattern 2: for each year, `present = rows where windowDays.has(r.doy) && metric(r) != null`; include the year if `present / windowDays.size >= minCoverage`. Return sorted ascending.
    Implement `effectiveN(qualifying)`: `{ n: qualifying.length, sufficient: qualifying.length >= 3 }`.
    Write `window.test.ts` and `coverage.test.ts` encoding every behavior bullet above (name the leap test "leap day fold" and the coverage test "qualifying years coverage" and the N test "min N 3" to match the RESEARCH test map `-t` selectors). Add a boundary test at exactly 0.8 coverage (must qualify).
  </action>
  <verify>
    <automated>cd /Users/jonb/Projects/betravedur && npx vitest run packages/domain/test/window.test.ts packages/domain/test/coverage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run packages/domain -t "leap day fold"` passes
    - `npx vitest run packages/domain -t "qualifying years coverage"` passes
    - `npx vitest run packages/domain -t "min N 3"` passes
    - `grep -q "NOT_IMPLEMENTED" packages/domain/src/window.ts; test $? -ne 0` (no stub remains in window.ts)
    - `grep -q "NOT_IMPLEMENTED" packages/domain/src/coverage.ts; test $? -ne 0` (no stub remains in coverage.ts)
    - No date-library import: `grep -Eq "dayjs|luxon|date-fns|moment" packages/domain/src/window.ts; test $? -ne 0`
  </acceptance_criteria>
  <done>Window + coverage math implemented and green; leap-fold, >=80% gate, and N>=3 rule all tested; domain package still dependency-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Circular wind mean (350/10 case) + scalar speed + precip sum-then-average (missing != zero)</name>
  <files>packages/domain/src/wind.ts, packages/domain/src/precip.ts, packages/domain/test/wind.test.ts, packages/domain/test/precip.test.ts</files>
  <read_first>
    - /Users/jonb/Projects/betravedur/.planning/phases/01-data-access-domain-core/01-RESEARCH.md (Pattern 1 circular mean code; Pattern 3 + Pitfall 3 precip; Pitfall 4 350/10 bug)
    - /Users/jonb/Projects/betravedur/.planning/phases/01-data-access-domain-core/01-CONTEXT.md (wind direction unit-vector mean; precip sum-then-average, missing never zero)
    - /Users/jonb/Projects/betravedur/packages/domain/src/wind.ts (stub signatures)
    - /Users/jonb/Projects/betravedur/packages/domain/src/precip.ts (stub signature)
  </read_first>
  <behavior>
    - circularMeanDirection([{speed:1,dirDeg:350},{speed:1,dirDeg:10}]).dirDeg is within 0.5 of 0 (i.e. ~0 or ~360), NEVER ~180
    - circularMeanDirection([]) === null; samples with null speed/dir are filtered out
    - a near-cancelling set (e.g. equal-speed N and S) yields a very small resultantSpeed (caller can flag "breytileg átt")
    - scalarMeanSpeed([5, null, 7]) === 6 (nulls skipped, not treated as 0); scalarMeanSpeed([null]) === null
    - sumPerYearThenAverage: year A window sums to 10mm over present days, year B sums to 20mm -> average 15mm across the two qualifying years
    - a missing precip day (r=null) is SKIPPED in the sum, not added as 0 (a year with one null day sums the remaining days, never inflating dryness with a zero)
    - only years in `qualifying` contribute; a non-qualifying year is ignored
  </behavior>
  <action>
    Implement `circularMeanDirection(samples)` in `wind.ts` exactly per RESEARCH Pattern 1: filter null speed/dir; accumulate `u += speed*sin(rad)`, `v += speed*cos(rad)`; `dir = atan2(meanU, meanV)` in degrees, add 360 if negative; `resultantSpeed = hypot(meanU, meanV)`; return null if no usable samples.
    Implement `scalarMeanSpeed(speeds)`: mean of non-null entries; null if none.
    Implement `sumPerYearThenAverage(rowsByYear, windowDays, qualifying)` in `precip.ts` per RESEARCH Pattern 3 + Pitfall 3: for each year in `qualifying`, sum `r` over rows where `windowDays.has(r.doy) && r.r != null` (skip nulls, never coerce to 0); then average those per-year sums across the qualifying years. Return null if `qualifying` is empty. Add a code comment noting the coverage gate (>=80%) already bounds the residual under-count bias (Pitfall 3) — do NOT scale/impute.
    Write `wind.test.ts` with the named test `"circular mean 350 10"` asserting result within 0.5 of 0/360 and explicitly asserting it is NOT within 10 of 180; add `"scalar wind speed"` test. Write `precip.test.ts` with `"precip missing not zero"` asserting a null day does not reduce the mean toward zero (compare against the same year without the null day) and the sum-then-average example.
  </action>
  <verify>
    <automated>cd /Users/jonb/Projects/betravedur && npx vitest run packages/domain/test/wind.test.ts packages/domain/test/precip.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run packages/domain -t "circular mean 350 10"` passes and its assertion set includes a NOT-180 check
    - `npx vitest run packages/domain -t "scalar wind speed"` passes
    - `npx vitest run packages/domain -t "precip missing not zero"` passes
    - `grep -q "atan2" packages/domain/src/wind.ts` succeeds (vector mean, not arithmetic)
    - `grep -q "NOT_IMPLEMENTED" packages/domain/src/wind.ts; test $? -ne 0` and same for `precip.ts` (no stubs remain)
    - full domain suite green: `npx vitest run packages/domain` exits 0
  </acceptance_criteria>
  <done>Circular mean (named 350/10 case), scalar speed, and honest precip aggregation implemented and green; DATA-05 fully satisfied at the math level.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| observation rows -> domain math | Rows carry null/implausible values; math must not silently corrupt averages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | precip.ts null handling | mitigate | Nulls skipped in sum (never coerced to 0); unit-tested "precip missing not zero" so a data gap cannot inflate dryness. |
| T-01-06 | Tampering | wind.ts direction mean | mitigate | Unit-vector mean prevents the 350/10 arithmetic bug that would point every marker arrow wrong; named regression test. |
| T-01-07 | Tampering | coverage.ts N inflation | mitigate | N derives from actual >=80% coverage, not the picker range; a sparse year cannot masquerade as a full year, preventing dishonest "meðaltal N ára". |
| T-01-08 | Tampering | supply chain (domain pkg) | mitigate | Domain package remains dependency-free (grep gate: no date-library import); zero third-party attack surface in the math layer. |
</threat_model>

<verification>
- `npx vitest run packages/domain` fully green.
- Named tests pass: "circular mean 350 10", "leap day fold", "qualifying years coverage", "min N 3", "precip missing not zero", "scalar wind speed".
- No `NOT_IMPLEMENTED` remains in window/coverage/wind/precip.
- Domain package still has zero runtime dependencies.
</verification>

<success_criteria>
- Circular mean of 350/10 approx 0, never 180 (named success criterion of the phase).
- Coverage-honest N (>=80%, N>=3) implemented and tested.
- Precip missing treated as missing, never zero.
- Feb-29 folded; leap/non-leap map the same window.
</success_criteria>

<output>
Create `.planning/phases/01-data-access-domain-core/01-02-SUMMARY.md` when done.
</output>
