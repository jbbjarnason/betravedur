---
phase: 01-data-access-domain-core
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - packages/fetch/src/client.ts
  - packages/fetch/src/observations.ts
  - packages/fetch/src/stations.ts
  - packages/fetch/src/registry.ts
  - packages/domain/src/attribution.ts
  - packages/domain/test/attribution.test.ts
  - packages/fetch/test/observations.test.ts
  - packages/fetch/test/registry.test.ts
  - packages/fetch/test/fixtures/aws-day.json
  - packages/fetch/test/fixtures/synop-day.json
  - packages/fetch/test/fixtures/stations.json
  - packages/fetch/test/fixtures/error-404.json
  - packages/fetch/test/fixtures/error-422.json
autonomous: false
requirements: [DATA-01, DATA-06, DATA-08]
user_setup: []

must_haves:
  truths:
    - "The fetch client parses live-shaped aws/day, synop/day, /stations, 404 and 422 fixtures offline and deterministically"
    - "aws/day rows keep r as null (no precip on AWS) and synop/day rows keep dv as null (no wind direction on SYNOP) after normalization"
    - "The station registry keys on integer station ID, retains decommissioned stations (ending != null), and never merges two IDs at the same physical place (Keflavík synop 990 vs AWS 1350 stay distinct)"
    - "Malformed / implausible observation values are dropped or nulled before reaching domain math, and a response missing the expected field set fails loudly"
    - "The CC BY 4.0 attribution text and source URL are available as a typed constant the UI can consume"
  artifacts:
    - path: "packages/fetch/src/observations.ts"
      provides: "fetchAwsDay/fetchSynopDay with schema-assert + normalize + clamp"
      contains: "fetchAwsDay"
    - path: "packages/fetch/src/registry.ts"
      provides: "buildRegistry (no-splice, keyed by ID, retains decommissioned)"
      contains: "station"
    - path: "packages/domain/src/attribution.ts"
      provides: "ATTRIBUTION constant with CC BY 4.0 text + source URL"
      contains: "CC BY 4.0"
    - path: "packages/fetch/test/fixtures/aws-day.json"
      provides: "live-captured AWS day fixture (r null, dv present)"
      contains: "\"r\": null"
  key_links:
    - from: "packages/fetch/src/observations.ts"
      to: "packages/domain/src/window.ts"
      via: "leapFoldedDoy sets DailyObservation.doy during normalization"
      pattern: "leapFoldedDoy"
    - from: "packages/fetch/src/registry.ts"
      to: "packages/fetch/src/stations.ts"
      via: "registry built from fetched StationMeta[]"
      pattern: "StationMeta"
---

<objective>
Harden the data-access layer to production quality: schema-assert and normalize the live api.vedur.is responses (validate the expected field set + `x-vi-api-version`, clamp/drop implausible values before they reach domain math), build the no-splice station registry keyed on integer station ID that retains decommissioned stations and never merges two IDs at one physical location, and record the verified CC BY 4.0 attribution text as a UI-consumable typed constant. All tests run offline against committed fixtures captured from the live API so the suite is deterministic and CI-safe.

Purpose: DATA-01 (correct fetch of all daily fields including the AWS-no-precip / SYNOP-no-winddir structural split), DATA-06 (registry that handles moves/closures/network churn without corrupting series), and DATA-08 (license compliance). This is the trust boundary where untrusted upstream JSON enters the system.
Output: Robust fetch/normalize/registry modules, committed live-shaped fixtures, and the attribution constant — the data-access half of the phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-data-access-domain-core/01-CONTEXT.md
@.planning/phases/01-data-access-domain-core/01-RESEARCH.md

<interfaces>
<!-- Contracts fixed by Plan 01 (packages/domain/src/types.ts). Normalize INTO DailyObservation / StationMeta. -->
<!-- DailyObservation: { station, date, doy, t, tx, tn, f, fx, fg, dv, r } (all metrics number|null) -->
<!-- StationMeta: { station, name, type:'sj'|'sk'|'ur'|'vf', owner, lat, lon, ele, start, ending:number|null } -->
<!-- attribution.ts stub (fill it): ATTRIBUTION { license, text_is, text_en, sourceUrl, modifiedNotice_is } -->
<!-- Live response shapes (2026-07-19):
     AWS day:  {station,name,time,t,tx,tn,f,fx,fg,dv,dv_txt,r:null,rsun:null,count_measurements}
     SYNOP day:{station,name,time,t,txx,tnn,f,fx,fg,sun,n,r,r_type}   (NO dv)
     /stations:{station,name,abbr,type,lat,lon,ele,wigos,owner,start,ending}
     Errors: 404 {"message":"No data found."} / {"message":"Station/s not found."}; 422 {"detail":[...]}
     Keflavík: synop 990 (from 1952) and AWS 1350 (from 2008) are DISTINCT stations -- never merge. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Package legitimacy gate for the four dev dependencies installed in Plan 01 (typescript@7.0.2, vitest@4.1.10, @vitest/coverage-v8@4.1.10, tsx@4.23.1). slopcheck could not be installed in this environment, so RESEARCH.md marks all four [ASSUMED]; protocol requires a blocking human verification before relying on them. This checkpoint is a formality — all four are first-party reference tooling with hundreds of millions of weekly downloads and empty postinstall scripts (verified) — but it cannot be auto-approved.</what-built>
  <how-to-verify>
    1. Confirm each package's registry page shows an official first-party source repo and expected publisher:
       - https://www.npmjs.com/package/typescript  (github.com/microsoft/TypeScript)
       - https://www.npmjs.com/package/vitest  (github.com/vitest-dev/vitest)
       - https://www.npmjs.com/package/@vitest/coverage-v8  (github.com/vitest-dev/vitest)
       - https://www.npmjs.com/package/tsx  (github.com/privatenumber/tsx)
    2. Confirm no unexpected postinstall from the installed tree:
       run `npm ls typescript vitest @vitest/coverage-v8 tsx` and, if desired, `cat node_modules/{typescript,vitest,tsx}/package.json | grep -i postinstall` (expect none).
    3. Versions match RESEARCH pins: typescript 7.0.2, vitest 4.1.10, @vitest/coverage-v8 4.1.10, tsx 4.23.1.
  </how-to-verify>
  <resume-signal>Type "approved" once the four packages are confirmed legitimate and postinstall-free, or name any package to remove/replace.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Capture live fixtures + schema-assert/normalize the observation fetchers</name>
  <files>packages/fetch/src/client.ts, packages/fetch/src/observations.ts, packages/fetch/test/observations.test.ts, packages/fetch/test/fixtures/aws-day.json, packages/fetch/test/fixtures/synop-day.json, packages/fetch/test/fixtures/error-404.json, packages/fetch/test/fixtures/error-422.json</files>
  <read_first>
    - /Users/jonb/Projects/betravedur/.planning/phases/01-data-access-domain-core/01-RESEARCH.md (Code Examples fetch; verified response shapes; API Reference table; Security Domain V5 controls; Pitfall 1/2 AWS-precip null semantics)
    - /Users/jonb/Projects/betravedur/packages/fetch/src/observations.ts (skeleton normalizer from Plan 01 to harden)
    - /Users/jonb/Projects/betravedur/packages/fetch/src/client.ts (fetchWithRetry from Plan 01)
    - /Users/jonb/Projects/betravedur/packages/domain/src/types.ts (DailyObservation target shape)
    - /Users/jonb/Projects/betravedur/packages/domain/src/window.ts (leapFoldedDoy for doy normalization)
  </read_first>
  <behavior>
    - Parsing the committed aws-day fixture yields DailyObservation[] where every row has r === null (AWS has no precip) and dv is a number (wind dir present)
    - Parsing the committed synop-day fixture yields rows where r is a number (precip present) and dv === null (SYNOP has no wind direction)
    - Each normalized row has a leap-folded `doy` set from its `time`/`date`; a Feb-29 row is dropped (leapFoldedDoy returns null)
    - A response object shaped like error-404 / error-422 does NOT parse as observations: it is detected and surfaced as an empty result or a typed error, never as a row with garbage fields
    - A row with an implausible temperature (e.g. t = 999) is clamped to null (dropped for that metric), not passed through to averages
    - A response whose rows are missing the expected field set (schema drift) throws loudly rather than silently normalizing
  </behavior>
  <action>
    Capture real fixtures by calling the live API and saving the JSON verbatim (use `curl` or a one-off `tsx` script; commit the output). Required fixtures matching RESEARCH verified shapes:
      - `aws-day.json`: `GET /observations/aws/day?station_id=1350&day_from=2024-07-15&day_to=2024-07-25&parameters=basic&format=json` (must contain `"r": null` and numeric `dv`).
      - `synop-day.json`: `GET /observations/synop/day?station_id=1&day_from=2024-09-01&day_to=2024-09-10&parameters=basic&format=json` (must contain numeric `r` and NO `dv`).
      - `error-404.json`: body `{"message":"No data found."}`. `error-422.json`: a real 422 `{"detail":[...]}` body.
    Harden `observations.ts`: add a `assertObservationSchema(rows, kind)` that verifies the expected field set is present (AWS: t,f,dv,r,time,station; SYNOP: t,f,r,time,station) and throws `Error("SCHEMA_DRIFT: ...")` on mismatch (Security Domain V5). If the top-level body matches an error shape (`message` or `detail` key), return `[]` for 404-style "no data" and throw for 422-style bad-request. Normalize each row into `DailyObservation`: map `time`->`date`, set `doy = leapFoldedDoy(date)` and DROP the row if `doy === null` (Feb 29); coerce absent fields to `null`; keep AWS `r=null`, SYNOP `dv=null`. Clamp implausible values to null before returning: temp outside [-60, 45] °C, wind speed outside [0, 120] m/s, dir outside [0, 360), precip < 0.
    Keep the `client.ts` retry/backoff from Plan 01; optionally read and assert the `x-vi-api-version` response header equals the researched `2026-02-17` and warn (not throw) on mismatch.
    Write `observations.test.ts` loading the committed fixtures from disk (no network) covering every behavior bullet; name the core test `"parse observations"` to match the RESEARCH test map selector.
  </action>
  <verify>
    <automated>cd /Users/jonb/Projects/betravedur && npx vitest run packages/fetch/test/observations.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run packages/fetch -t "parse observations"` passes
    - `grep -q '"r": null' packages/fetch/test/fixtures/aws-day.json` succeeds (real AWS-no-precip fixture captured)
    - AWS fixture has no top-level `dv:null` requirement but SYNOP fixture must lack dv: `grep -q '"dv"' packages/fetch/test/fixtures/synop-day.json; test $? -ne 0`
    - `grep -Eq "SCHEMA_DRIFT|assertObservationSchema" packages/fetch/src/observations.ts` succeeds (schema-assert present)
    - `grep -q "leapFoldedDoy" packages/fetch/src/observations.ts` succeeds (doy normalization wired to domain)
    - a test asserting an implausible-temp row is nulled/dropped passes
  </acceptance_criteria>
  <done>Observation fetchers schema-assert, normalize, and clamp against committed live fixtures offline; AWS-no-precip and SYNOP-no-winddir preserved; DATA-01 satisfied.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: No-splice station registry + CC BY 4.0 attribution constant</name>
  <files>packages/fetch/src/stations.ts, packages/fetch/src/registry.ts, packages/domain/src/attribution.ts, packages/domain/test/attribution.test.ts, packages/fetch/test/registry.test.ts, packages/fetch/test/fixtures/stations.json</files>
  <read_first>
    - /Users/jonb/Projects/betravedur/.planning/phases/01-data-access-domain-core/01-RESEARCH.md (DATA-06 rationale; Pitfall 6 no-splice; /stations schema; Sources: athuganir.vedur.is/disclaimer CC BY 4.0 wording)
    - /Users/jonb/Projects/betravedur/.planning/phases/01-data-access-domain-core/01-CONTEXT.md (registry keyed on ID, active-date windows, never merge relocations; attribution must be UI-consumable)
    - /Users/jonb/Projects/betravedur/packages/fetch/src/stations.ts (skeleton fetchStations from Plan 01)
    - /Users/jonb/Projects/betravedur/packages/domain/src/attribution.ts (stub to fill)
    - /Users/jonb/Projects/betravedur/packages/domain/src/types.ts (StationMeta)
  </read_first>
  <behavior>
    - buildRegistry keys on integer station ID: the result maps id -> StationMeta with no collision-merge
    - Keflavík synop 990 and AWS 1350 both appear as separate entries; there is NO merged "Keflavík" record splicing their histories
    - a decommissioned station (ending != null) is RETAINED in the registry (needed for historical windows), not filtered out
    - each entry carries its active-date window via start (year) and ending (year|null)
    - ATTRIBUTION.license === "CC BY 4.0"; ATTRIBUTION.text_is is non-empty Icelandic attribution naming Veðurstofa Íslands; ATTRIBUTION.sourceUrl points at the vedur.is data/terms source; modifiedNotice_is states the data is modified/aggregated (CC BY 4.0 modified-data clause)
  </behavior>
  <action>
    Capture `stations.json` fixture: `GET /stations?station_id=990&station_id=1350&station_id=1` (Keflavík synop + Keflavík AWS + Reykjavík) saved verbatim — this fixture proves the no-splice case with two Keflavík IDs.
    Implement `stations.ts` `fetchStations(stationIds)` -> `StationMeta[]` mapping `/stations` fields; coerce `ending` to `number|null`.
    Implement `registry.ts` `buildRegistry(stations: StationMeta[]): Map<number, StationMeta>` (or `Record<number, StationMeta>`) keyed strictly on `station` id; MUST NOT collapse by name/location; MUST retain entries with non-null `ending`. Add an optional `writeRegistry(stations, path)` helper that serializes the sorted registry to JSON (the committed `stations.json` artifact the pipeline will regenerate) — but do not write into the repo during tests.
    Fill `packages/domain/src/attribution.ts` `ATTRIBUTION` with the verified CC BY 4.0 wording from RESEARCH Sources (athuganir.vedur.is/disclaimer, both `is`/`en`): `license: "CC BY 4.0"`, `text_is` crediting "Veðurstofa Íslands", `text_en` English credit, `sourceUrl` the disclaimer/data URL, `modifiedNotice_is` noting the data has been aggregated/modified (required by the CC BY modified-data clause). Keep it a plain typed const, zero deps (stays in the pure domain package for UI consumption per the Architectural Responsibility Map).
    Write `registry.test.ts` (named `"registry no splice"`) loading `stations.json`: assert 990 and 1350 are distinct entries, assert a decommissioned station is retained, assert keying by ID. Write `attribution.test.ts` (named `"attribution"`) asserting license and non-empty is/en text + sourceUrl.
  </action>
  <verify>
    <automated>cd /Users/jonb/Projects/betravedur && npx vitest run packages/fetch/test/registry.test.ts packages/domain/test/attribution.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run packages/fetch -t "registry no splice"` passes
    - `npx vitest run packages/domain -t "attribution"` passes
    - `grep -q "CC BY 4.0" packages/domain/src/attribution.ts` succeeds
    - registry test asserts stations 990 and 1350 are BOTH present as separate keys (no merge)
    - registry test asserts an `ending != null` station is retained
    - `grep -q "NOT_IMPLEMENTED" packages/domain/src/attribution.ts; test $? -ne 0` (stub filled)
  </acceptance_criteria>
  <done>Registry keyed on ID with no splicing and decommissioned retention; CC BY 4.0 attribution constant available to the UI; DATA-06 and DATA-08 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| api.vedur.is JSON -> fetch/normalize | Untrusted upstream response crosses into the system; primary tampering surface |
| npm registry -> dev toolchain | Third-party packages relied upon (legitimacy gate) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Tampering | observations.ts schema drift | mitigate | `assertObservationSchema` verifies expected field set + warns on `x-vi-api-version` mismatch; throws `SCHEMA_DRIFT` rather than silently corrupting aggregates (Security Domain V5). |
| T-01-10 | Tampering | observations.ts malformed values | mitigate | Range-clamp temp/wind/precip and drop malformed rows in the normalizer before domain math sees them. |
| T-01-11 | Tampering | registry.ts ID splicing | mitigate | Registry keyed strictly on integer ID; Keflavík 990 vs 1350 stay distinct; decommissioned retained — a churned/relocated station cannot bake a discontinuity into averages (DATA-06). |
| T-01-12 | Information disclosure | committed fixtures / repo | mitigate | Keyless public API; fixtures contain only public open data; assert no credential ever committed. |
| T-01-SC | Tampering | npm installs (typescript/vitest/coverage-v8/tsx) | mitigate | Blocking human legitimacy checkpoint (Task above); [ASSUMED] packages, empty postinstall verified; not auto-approvable. |
</threat_model>

<verification>
- `npx vitest run packages/fetch packages/domain` green (offline, fixture-driven).
- Named tests pass: "parse observations", "registry no splice", "attribution".
- AWS fixture has `r:null`; SYNOP fixture lacks `dv`.
- Registry keeps 990 and 1350 distinct and retains decommissioned stations.
- Legitimacy checkpoint approved.
</verification>

<success_criteria>
- Real daily observations fetched + schema-asserted + normalized for AWS and SYNOP (DATA-01).
- Station registry keyed on ID, no splicing, decommissioned retained (DATA-06).
- CC BY 4.0 attribution documented in a UI-consumable constant (DATA-08).
</success_criteria>

<output>
Create `.planning/phases/01-data-access-domain-core/01-03-SUMMARY.md` when done.
</output>
