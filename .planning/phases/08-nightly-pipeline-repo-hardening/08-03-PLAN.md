---
phase: 08-nightly-pipeline-repo-hardening
plan: 03
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - .github/workflows/squash-reset.yml
  - PIPELINE.md
  - pipeline/test/squash-workflow.test.ts
autonomous: true
requirements: [DATA-03]
must_haves:
  truths:
    - "A documented squash-reset cadence bounds .git growth; an optional monthly workflow_dispatch job automates it"
    - "The squash-reset force-pushes ONLY origin data, after asserting the current branch is data — never main"
    - "PIPELINE.md documents the two live prerequisites: push repo to GitHub + set Pages source = GitHub Actions; and the optional HEARTBEAT_URL secret"
  artifacts:
    - path: ".github/workflows/squash-reset.yml"
      provides: "manual/monthly squash-reset job, force-push scoped to data with a branch assertion"
      contains: "push --force origin data"
    - path: "PIPELINE.md"
      provides: "Phase 8 §: prerequisites, nightly operation, squash-reset cadence"
      contains: "GitHub Actions"
    - path: "pipeline/test/squash-workflow.test.ts"
      provides: "gate: force-push scoped to data only + branch assertion, never main"
      contains: "push --force origin data"
  key_links:
    - from: ".github/workflows/squash-reset.yml"
      to: "git push --force origin data"
      via: "branch-assertion guard before force-push"
      pattern: "rev-parse --abbrev-ref HEAD.*=.*data|abbrev-ref HEAD"
---

<objective>
Repo hardening (DATA-07 continuation under DATA-03): implement the squash-reset as an optional manual/monthly workflow with force-push scoped to `data` only (guarded by a branch assertion), and document the operator story in PIPELINE.md — the two hard live prerequisites (push repo to GitHub + enable Pages=Actions), nightly operation, and the squash-reset cadence.

Purpose: Nightly commits must not balloon `.git` history over years of unattended runs. The byte-identical-tree squash-reset was proven in Phase 2; this plan automates its cadence safely and records the prerequisites the first live run needs.

Output: `.github/workflows/squash-reset.yml`, updated `PIPELINE.md`, and a force-push-scoping test. Zero new npm deps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-nightly-pipeline-repo-hardening/08-CONTEXT.md
@.planning/phases/08-nightly-pipeline-repo-hardening/08-RESEARCH.md
@PIPELINE.md

<interfaces>
<!-- The Phase-2 squash-reset recipe (PIPELINE.md §6), verified byte-identical-tree (02-04 E7). -->
```bash
cd ./data-wt
test "$(git rev-parse --abbrev-ref HEAD)" = "data"   # ASSERT branch before any force-push
git checkout --orphan data-fresh
git add -A
git commit -m "data: squashed backfill $(date +%F)"
git branch -D data
git branch -m data-fresh data
git push --force origin data     # ONLY data, explicit ref, asserted branch — NEVER main
```
Live prerequisites (RESEARCH Runtime State Inventory / A4):
- The repo has NO remote locally (`git remote -v` empty) — must be pushed to GitHub first (main + data).
- GitHub Pages must be set to Source = "GitHub Actions" (not "deploy from a branch"); github-pages environment.
- HEARTBEAT_URL is an OPTIONAL repo secret; absent = heartbeat is a no-op, run stays green.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Squash-reset workflow (manual + optional monthly, force-push scoped to data)</name>
  <files>.github/workflows/squash-reset.yml</files>
  <read_first>PIPELINE.md (§6 The data branch), .planning/phases/08-nightly-pipeline-repo-hardening/08-RESEARCH.md (Periodic squash-reset code example)</read_first>
  <action>
    Create `.github/workflows/squash-reset.yml`: triggers `workflow_dispatch` (manual) plus an optional monthly `schedule` cron (off-peak, e.g. `17 5 1 * *`). Single job on ubuntu-latest, `permissions: { contents: write }` only (no pages/id-token — this job does not deploy). Steps: checkout@v4; materialize the data worktree (`git fetch origin data --depth=1`; `git worktree add ./data-wt data`); run the squash-reset recipe from the interfaces block INSIDE ./data-wt with the branch assertion `test "$(git rev-parse --abbrev-ref HEAD)" = "data"` BEFORE any force-push; the only force-push is `git push --force origin data` (explicit ref). NEVER reference main; no other `--force`. Add a comment header stating this is force-push-owned by the pipeline, data-only, per RESEARCH Pitfall 3. Concurrency `group: betravedur-data-branch, cancel-in-progress: false` (shares the nightly group so a squash never races a nightly push).
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/squash-reset.yml','utf8').split('\n').filter(l=>!l.trimStart().startsWith('#')).join('\n'); if(!/push --force origin data\b/.test(y)) throw new Error('scoped force-push missing'); if(/--force\s+origin\s+main|push\s+.*\bmain\b/.test(y)) throw new Error('touches main'); if(!/abbrev-ref HEAD/.test(y)) throw new Error('branch assertion missing'); console.log('squash yaml ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/squash-reset.yml` exists and parses (Task 3 test loads it).
    - `grep -c 'push --force origin data' .github/workflows/squash-reset.yml` ≥ 1.
    - `grep -c 'abbrev-ref HEAD' .github/workflows/squash-reset.yml` ≥ 1 (branch assertion present).
    - `grep -v '^#' .github/workflows/squash-reset.yml | grep -c 'main'` == 0 (never references main).
    - `grep -v '^#' .github/workflows/squash-reset.yml | grep -c 'pages:'` == 0 (no deploy permissions).
  </acceptance_criteria>
  <done>squash-reset.yml runs manually or monthly, asserts branch==data, force-pushes only origin data, never touches main, contents:write only.</done>
</task>

<task type="auto">
  <name>Task 2: Document Phase 8 operation + live prerequisites in PIPELINE.md</name>
  <files>PIPELINE.md</files>
  <read_first>PIPELINE.md (all sections), .planning/phases/08-nightly-pipeline-repo-hardening/08-RESEARCH.md (Runtime State Inventory, Common Pitfalls)</read_first>
  <action>
    Append a "## 8. Nightly automation (Phase 8)" section to PIPELINE.md covering: (a) PREREQUISITES for the first live run — the repo currently has no remote (`git remote -v` empty); it must be pushed to GitHub (main AND the local-only `data` branch's first push), and Pages Settings → Source must be "GitHub Actions" (not deploy-from-branch); the github-pages environment. (b) How nightly.yml works — off-peak cron `37 4 * * *`, incremental resume from per-station high-water, skip-empty (no commit when no new data), touched-only aggregate, `--root ./data-wt` worktree wiring, deploy via actions/deploy-pages@v4. (c) The full national backfill trigger — `workflow_dispatch` with `full_backfill: true` (run ONCE to seed the national set; documented one-command, not run during this phase). (d) Monitoring — optional HEARTBEAT_URL repo secret (healthchecks.io-style), degrades to a no-op when unset; plus the Actions status UI; note the 60-day auto-disable and that a successful nightly commit resets the timer. (e) Squash-reset cadence — recommend documented-manual, with the optional monthly `squash-reset.yml` workflow_dispatch; restate the force-push-scoped-to-data-only safety rule. Keep the existing §1–§7 intact.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('PIPELINE.md','utf8'); for(const k of ['GitHub Actions','full_backfill','HEARTBEAT_URL','37 4','squash-reset','data-wt']){ if(!t.includes(k)) throw new Error('missing: '+k);} console.log('pipeline doc ok')"</automated>
  </verify>
  <acceptance_criteria>
    - PIPELINE.md contains a "## 8." section (`grep -c '## 8' PIPELINE.md` ≥ 1).
    - Mentions all of: `GitHub Actions` (Pages source), `full_backfill`, `HEARTBEAT_URL`, `37 4` (cron), `squash-reset`, `data-wt` (`node` check above passes).
    - Documents the "push repo to GitHub" + "data branch first push" prerequisites (`grep -ci 'remote\|first push\|push.*github' PIPELINE.md` ≥ 1).
    - Existing §1–§7 headers still present (`grep -c '## [1-7]\.' PIPELINE.md` ≥ 7).
  </acceptance_criteria>
  <done>PIPELINE.md documents the two live prerequisites, nightly operation, full_backfill trigger, heartbeat monitoring, and squash-reset cadence; existing sections intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Force-push-scoping gate test</name>
  <files>pipeline/test/squash-workflow.test.ts</files>
  <read_first>.github/workflows/squash-reset.yml, pipeline/test/workflow.test.ts (if present from Plan 02, mirror the text-assertion style)</read_first>
  <behavior>
    - Reads .github/workflows/squash-reset.yml as text (no yaml dep), filters comment lines.
    - Asserts a branch assertion `git rev-parse --abbrev-ref HEAD` compared to `data` precedes any force-push.
    - Asserts the ONLY force-push is `git push --force origin data` (explicit ref).
    - Asserts NO reference to main and NO `--force` targeting anything but `origin data`.
    - Asserts permissions do NOT include pages/id-token (squash job never deploys).
  </behavior>
  <action>
    Create `pipeline/test/squash-workflow.test.ts` (vitest) that reads squash-reset.yml as text, strips comment lines, and asserts every property in the behavior block via string/regex. Use a line-index check so the branch assertion appears before the force-push line. This is the DATA-03/DATA-07 branch-corruption guard from RESEARCH threat T-08-10.
  </action>
  <verify>
    <automated>npx vitest run pipeline/test/squash-workflow.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pipeline/test/squash-workflow.test.ts` exists; `grep -c 'push --force origin data' pipeline/test/squash-workflow.test.ts` ≥ 1.
    - Test asserts: branch-assertion-before-force-push ordering, force-push scoped to `origin data` only, no `main` reference, no pages/id-token permission.
    - `npx vitest run pipeline/test/squash-workflow.test.ts` green.
    - No new package.json dependency.
  </acceptance_criteria>
  <done>The squash-workflow test green, proving the force-push is guarded, scoped to data, and never touches main; zero new deps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| squash-reset job → git remote | a mis-scoped force-push could clobber main or the live history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Tampering | force-push clobbers main/live | mitigate | Branch assertion `abbrev-ref HEAD == data` before force-push; only `git push --force origin data`; squash-workflow.test asserts no main reference and scoped ref; shares nightly concurrency group so no race |
| T-08-11 | DoS | unbounded .git growth over years | mitigate | Documented squash-reset cadence + optional monthly automation; byte-identical-tree squash proven Phase 2 (02-04 E7) |
| T-08-14 | Elevation | squash job over-privileged | mitigate | `permissions: contents: write` only (no pages/id-token); test asserts absence |
| T-08-SC | Tampering | unpinned actions | mitigate | checkout pinned @v4; zero new npm deps (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `npx vitest run` full suite green (includes squash-workflow.test.ts).
- squash-reset.yml + PIPELINE.md doc assertions pass.
- No new package.json dependencies.
</verification>

<success_criteria>
Squash-reset automated as an optional manual/monthly job, force-push provably scoped to `data` (branch-asserted, never main), and PIPELINE.md documents the two live prerequisites, nightly operation, full_backfill trigger, heartbeat, and squash cadence. Full suite green, zero new deps.
</success_criteria>

<output>
Create `.planning/phases/08-nightly-pipeline-repo-hardening/08-03-SUMMARY.md` when done.
</output>
