---
phase: 06-station-chart-panel
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - site/src/ui/chartPanel.ts
  - site/src/ui/stationPanel.ts
  - site/tests/e2e/panel.spec.ts
autonomous: true
requirements: [CHART-01, CHART-02]

must_haves:
  truths:
    - "First panel open dynamically imports chartPanel.ts → ECharts loads in a separate Vite chunk (never the entry bundle)"
    - "Temp + wind figures render an ECharts BOXPLOT series ([min,p10,p50,p90,max] per doy), single neutral tone, NO directional green/red coloring"
    - "Precip figure renders BARS (per-doy median total), with explicit gaps for missing doys (never a zero bar)"
    - "Charts use distribution semantics; the temperature series uses --chart-temp, never --accent or --score-*"
    - "prefers-reduced-motion → ECharts animation:false"
    - "Each chart figure exposes an accessible text/aria-label distribution summary (canvas is not the sole data carrier)"
  artifacts:
    - path: "site/src/ui/chartPanel.ts"
      provides: "The lazy chunk: echarts/core à-la-carte, boxplot+bar option builders, aria summaries"
      contains: "boxplot"
  key_links:
    - from: "site/src/ui/stationPanel.ts"
      to: "site/src/ui/chartPanel.ts"
      via: "dynamic import() on first open"
      pattern: "import\\(\"\\./chartPanel"
    - from: "site/src/ui/chartPanel.ts"
      to: "echarts/core"
      via: "à-la-carte import + echarts.use"
      pattern: "echarts/core"
---

<objective>
Complete the vertical slice: replace the `hleð riti…` seam with the real lazy-loaded ECharts charts — distribution boxplots for temperature and wind and precipitation bars — rendered from the Plan 01 distribution helper, isolated to a dynamically-imported chunk so the initial map load never pays for ECharts.

Purpose: This is the CHART-01/CHART-02 payload — honest distribution semantics (boxplot, NOT candlestick; no directional color) with the mandatory reading keys already in the DOM (Plan 02) and an accessible summary so the canvas is never the sole data carrier. It flips the remaining panel.spec criteria (2, 3, 11, 12, 14) and the build-size chunk-split gate green.
Output: `chartPanel.ts` (the à-la-carte echarts lazy chunk with boxplot/bar builders + aria summaries), the stationPanel seam wired to `import('./chartPanel.js')`, and the final panel.spec criteria + build gate green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-station-chart-panel/06-UI-SPEC.md
@.planning/phases/06-station-chart-panel/06-RESEARCH.md
@.planning/phases/06-station-chart-panel/06-01-SUMMARY.md
@.planning/phases/06-station-chart-panel/06-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 (see 06-01-SUMMARY for the exact export path) — the chart INPUT: -->
```typescript
// perDoyDistribution result (sufficient case):
type PerDoyBox = { doy: number; min: number; max: number; p10: number; p50: number; p90: number } | { doy: number; missing: true };
type DistributionResult = { sufficient: false } | { sufficient: true; n: number; perDoy: PerDoyBox[] };
// precip variant → per-doy median total or { missing:true }.
```

<!-- From Plan 02 stationPanel: the seam to fill (renderChartInto stub → real dynamic import). -->
```typescript
// stationPanel.ts exposes a per-figure render seam. Plan 03 replaces the `hleð riti…` stub
// with: await import("./chartPanel.js"); then chartMod.renderBoxplot(figureCanvasEl, spec) etc.
```

<!-- RESEARCH Pattern 1 — the EXACT à-la-carte import set (minimal, tree-shaken, ~80-130KB gzip target A4): -->
```typescript
import * as echarts from "echarts/core";
import { BoxplotChart, BarChart } from "echarts/charts";
import { GridComponent, TooltipComponent, TitleComponent } from "echarts/components";
import { CanvasRenderer } from "echarts/renderers";
import type { ComposeOption } from "echarts/core";
import type { BoxplotSeriesOption, BarSeriesOption } from "echarts/charts";
import type { GridComponentOption, TooltipComponentOption, TitleComponentOption } from "echarts/components";
echarts.use([ BoxplotChart, BarChart, GridComponent, TooltipComponent, TitleComponent, CanvasRenderer ]);
```

<!-- Boxplot data mapping (RESEARCH Pattern 2): our distribution → [min,Q1,median,Q3,max]-shaped 5-number: -->
```
box = [d.min, d.p10, d.p50, d.p90, d.max]   // box spans p10..p90, median line p50, whiskers min/max
// single neutral itemStyle (var-resolved --chart-temp / --chart-wind); NO color0, NO candlestick, NO up/down.
```

<!-- x-axis order (RESEARCH Pitfall 4): use [...expandWindow(window)] insertion order, NOT numeric 1..365. -->
</interfaces>

<constraints>
- ALL echarts imports (value AND type) live ONLY in chartPanel.ts. Nothing in main.ts / stationPanel.ts may import echarts (even `import type`), or it pulls into the entry chunk (RESEARCH Pitfall 6). The panel shell reaches it solely via `await import("./chartPanel.js")`.
- Use `echarts/core` + explicit pieces — NEVER `import * as echarts from "echarts"` (full build defeats the size target).
- boxplot, NOT candlestick (candlestick carries financial up/down color — the exact semantics CONTEXT forbids).
- Resolve the CSS token values (--chart-temp etc.) at runtime (getComputedStyle on :root) and pass the hex to the ECharts itemStyle — ECharts options take colors, not CSS var() strings.
- Reduced-motion: `animation: false` when matchMedia("(prefers-reduced-motion: reduce)").matches (research recommends animation:false unconditionally for these static charts — acceptable).
- ECharts `aria: { enabled: true }` AND a visually-hidden per-figure summary/table so screen readers get the numbers (RESEARCH Pitfall 5). Tooltip formatters must NOT interpolate untrusted HTML (V11) — plain strings only.
- Chunk-load failure → the figure shows `engin gögn fyrir þetta tímabil` (defensive), never a hang/throw.
</constraints>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: chartPanel.ts — à-la-carte ECharts boxplot + bar builders with aria summaries</name>
  <files>site/src/ui/chartPanel.ts</files>
  <read_first>
    - .planning/phases/06-station-chart-panel/06-RESEARCH.md Pattern 1/2/3 + Pitfall 1/3/4/5/6 + Code Examples
    - .planning/phases/06-station-chart-panel/06-UI-SPEC.md "Distribution Candlestick Spec", "Precipitation Bar Spec", "Accessibility Notes"
    - 06-01-SUMMARY.md (exact perDoyDistribution export path + result type)
    - site/src/ui/stationPanel.ts (the renderChartInto seam signature to satisfy; the figure/canvas DOM Plan 02 created)
  </read_first>
  <behavior>
    - chartPanel.ts registers the minimal à-la-carte echarts set and exports builder(s), e.g. renderBoxplot(container, { perDoy, unit, tone, orderedLabels, ariaSummary }) and renderBars(container, { perDoy, orderedLabels, ariaSummary }).
    - Boxplot: category x-axis in expandWindow window order with Icelandic short-date tick labels (thinned at 30-day width so ticks don't collide); y-axis in °C (temp) / m/s (wind, zero-floored); one series, `type:"boxplot"`, data = [min,p10,p50,p90,max] per non-missing doy; single neutral itemStyle = the resolved --chart-temp / --chart-wind hex (NO color0, NO up/down). animation:false under reduced-motion. aria.enabled true.
    - Bars: `type:"bar"`, per-doy median precip total, y in mm from 0; missing doys render as an explicit GAP (no bar / null datum), NEVER a zero-height bar. tone = --chart-precip.
    - Each render also writes a visually-hidden text summary / table into the figure (e.g. `Hiti: dæmigert bil {p10}–{p90}°C, dæmigerður dagur {median}°C, frá {min}°C til {max}°C`) and sets the canvas container role="img" + aria-label mirroring the summary. All numbers Icelandic comma-decimal via the shared locale formatter.
    - Failure/empty guard: if perDoy is empty/insufficient the builder writes the `engin gögn fyrir þetta tímabil` text rather than an empty chart (defensive; matches Plan 02 no-data path).
  </behavior>
  <action>
    Create `site/src/ui/chartPanel.ts` with the exact à-la-carte import block + `echarts.use([...])` from the interfaces (do NOT register Legend/Toolbox/MarkLine — keep the tree minimal for the A4 size target). Implement `renderBoxplot` and `renderBars` per the behavior, mapping perDoyDistribution → [min,p10,p50,p90,max] boxes and per-doy median precip bars, x-axis via `[...expandWindow(window)]` order with Icelandic date labels, tones resolved from getComputedStyle(:root) for --chart-temp/--chart-wind/--chart-precip. Set animation:false (reduced-motion), aria:{enabled:true}, and write the visually-hidden per-figure distribution summary + role="img"/aria-label. Tooltip formatter (if any) returns plain Icelandic strings, never HTML. Confine every echarts reference to this file.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p site</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "echarts/core" site/src/ui/chartPanel.ts` and `grep -q '"boxplot"\|type: *"boxplot"' site/src/ui/chartPanel.ts`
    - NO candlestick / directional color: `grep -c "candlestick\|color0" site/src/ui/chartPanel.ts` returns 0
    - NO full-build import: `grep -c 'from "echarts"$\|from "echarts";' site/src/ui/chartPanel.ts` returns 0 (only echarts/core + echarts/charts etc.)
    - `grep -q "chart-temp\|chart-wind\|chart-precip" site/src/ui/chartPanel.ts` (tones resolved)
    - `grep -q "animation" site/src/ui/chartPanel.ts` (animation:false) and `grep -q "aria" site/src/ui/chartPanel.ts`
    - `grep -q "role\|aria-label" site/src/ui/chartPanel.ts` (accessible summary)
    - echarts is NOT referenced outside chartPanel.ts: `grep -rn "echarts" site/src/main.ts site/src/ui/stationPanel.ts` returns nothing
    - `npx tsc --noEmit -p site` 0 errors
  </acceptance_criteria>
  <done>chartPanel.ts renders honest distribution boxplots (temp/wind, single neutral tone, no directional color) and precip bars (median total, explicit gaps) from the Plan 01 helper, with reduced-motion animation:false and an accessible per-figure summary — all echarts imports confined to this one file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the lazy import seam + flip remaining panel.spec criteria + build-size gate</name>
  <files>site/src/ui/stationPanel.ts, site/tests/e2e/panel.spec.ts</files>
  <read_first>
    - site/src/ui/stationPanel.ts (the renderChartInto / `hleð riti…` stub from Plan 02 to replace with the dynamic import)
    - .planning/phases/06-station-chart-panel/06-RESEARCH.md Pattern 3 (Vite auto code-split) + Pitfall 6 + A4
    - site/tests/e2e/panel.spec.ts (the remaining fixme criteria 2,3,11,12,14 + the build-size gate scaffolded in Plan 01)
  </read_first>
  <behavior>
    - stationPanel's per-figure render seam now does: for a SUFFICIENT metric, `const mod = await import("./chartPanel.js")` (memoized after first call), then mod.renderBoxplot/renderBars into the figure's canvas container; the `hleð riti…` placeholder shows only while the chunk is in flight; a chunk-load rejection falls back to the `engin gögn fyrir þetta tímabil` text.
    - Vite auto-splits `chartPanel.ts` (+ echarts) into a distinct chunk on the dynamic import — no manualChunks config. The entry/main chunk contains NO echarts.
    - panel.spec criteria 2 (three titled chart canvases), 3 (three reading keys — already DOM text from Plan 02, assert present), 11 (distribution-not-finance: grep-gate --chart-* used, --accent/--score- absent in chartPanel option module; assert canvases render for a data station), 12 (reduced-motion → animation:false), 14 (accessible summary present) turn green. Criterion 10's note (the one echarts JS chunk request is allowed, but zero data/ requests) holds.
    - Build-size gate: `npm run build` emits a distinct `chartPanel-*.js` (or hashed) chunk containing echarts; the entry chunk does NOT contain echarts (A4).
  </behavior>
  <action>
    Replace the Plan 02 `renderChartInto` stub in stationPanel.ts with the memoized `await import("./chartPanel.js")` call that renders boxplot/bar for sufficient metrics, keeping `hleð riti…` as the in-flight placeholder and the no-data text as the failure/insufficient fallback. Ensure NO static echarts import leaks into stationPanel.ts (only the dynamic import string). Un-fixme and implement panel.spec criteria 2, 3, 11, 12, 14 with the exact selectors from the Plan 01 scaffold (three `figure` canvases; the three reading-key substrings; the token grep-gate; reduced-motion animation:false via the ECharts option or a data attribute the panel sets; the visually-hidden summary text). Finalize the build-size gate (Plan 01 scaffolded it): assert `dist/assets` contains a chartPanel/echarts chunk AND the entry chunk lacks echarts — e.g. a small check that greps the built entry JS for an echarts marker and expects absence, and confirms a separate chunk exists. Run the full E2E + build.
  </action>
  <verify>
    <automated>cd site && npm run build && npx playwright test tests/e2e/panel.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'import(\"./chartPanel' site/src/ui/stationPanel.ts` (dynamic import present)
    - NO static echarts import in stationPanel.ts: `grep -c "from \"echarts" site/src/ui/stationPanel.ts` returns 0
    - panel.spec criteria 2, 3, 11, 12, 14 are active (not fixme) and green
    - Build-size gate green: a distinct chart/echarts chunk exists in `dist/assets` AND the entry chunk contains no echarts (assertion executes and passes)
    - `npm run build` succeeds; `npx tsc --noEmit -p site` 0 errors
    - FULL suite green: `npm run e2e -w site` (all panel criteria + shell/markers/selection/score, no regression)
  </acceptance_criteria>
  <done>The panel lazily loads ECharts on first open into a separate chunk (entry bundle echarts-free, A4 gate green); temp/wind boxplots + precip bars render for data stations with honest distribution semantics, reduced-motion animation off, and accessible summaries. All 14 UI-SPEC criteria and the build-size gate are green; no prior-phase regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm chunk → browser | ECharts code loaded lazily at runtime |
| distribution values → ECharts/tooltip | Numeric data + station-derived labels into chart options |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Tampering (XSS) | ECharts tooltip formatter | mitigate | Formatters return plain Icelandic strings, never interpolated HTML; canvas has no HTML injection surface (V11) |
| T-06-08 | DoS | chartPanel chunk load failure | mitigate | import() rejection → `engin gögn fyrir þetta tímabil` fallback; never hang/throw (V7) |
| T-06-09 | Tampering | echarts leak into entry bundle | mitigate | All echarts imports confined to chartPanel.ts; build-size gate asserts entry chunk echarts-free (A4/Pitfall 6) |
</threat_model>

<verification>
- All 14 UI-SPEC acceptance criteria green in panel.spec.
- Build-size gate: echarts in a lazy chunk, absent from the entry bundle.
- boxplot (not candlestick), single neutral tone, no --accent/--score- in the chart module.
- `npx tsc --noEmit -p site` 0 errors; `npm run e2e -w site` fully green (no Phase 3/4/5 regression).
</verification>

<success_criteria>
- Temp/wind distribution boxplots + precip bars render from the Plan 01 helper on first panel open, lazily loaded.
- Distribution semantics enforced (boxplot, single tone, no directional color, no accent/score tokens).
- Reduced-motion animation off; accessible per-figure summaries present.
- ECharts confined to a lazy chunk; entry bundle echarts-free (A4).
- Full E2E green including all 14 UI-SPEC criteria.
</success_criteria>

<output>
Create `.planning/phases/06-station-chart-panel/06-03-SUMMARY.md` when done.
</output>
