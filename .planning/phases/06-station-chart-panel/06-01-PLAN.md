---
phase: 06-station-chart-panel
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/domain/src/distribution.ts
  - packages/domain/src/index.ts
  - packages/domain/test/distribution.test.ts
  - site/src/data/daylight.ts
  - site/src/data/daylight.test.ts
  - site/package.json
  - site/package-lock.json
  - site/tests/e2e/panel.spec.ts
autonomous: true
requirements: [CHART-01, CHART-02, CHART-03, CHART-04]
user_setup: []

must_haves:
  truths:
    - "percentile(values,p) computes type-7 linear-interpolated quantiles (p0=min, p50=median, p100=max)"
    - "perDoyDistribution reshapes decoded rows into per-doy [min,p10,p50,p90,max] across qualifying years, in window order (wrap-correct)"
    - "A metric below the N>=3 / >=80% gate returns { sufficient:false } → 'engin gögn' path (CHART-04)"
    - "A doy with no qualifying values is emitted as { missing:true } — an explicit gap, never a zero box"
    - "daylightHours branches on alwaysUp/alwaysDown FIRST → no NaN at Iceland solstices (CHART-03)"
    - "echarts 6.1.0 + suncalc 2.0.1 install pinned, no postinstall scripts run"
    - "panel.spec.ts exists as a failing/fixme E2E skeleton covering the 14 UI-SPEC criteria"
    - "chart chunk build-size gate exists asserting echarts is NOT in the entry chunk"
  artifacts:
    - path: "packages/domain/src/distribution.ts"
      provides: "percentile + perDoyDistribution pure helpers"
      contains: "export function perDoyDistribution"
    - path: "packages/domain/test/distribution.test.ts"
      provides: "unit tests: percentile boundaries, qualifying-years filter, missing gap, wrap order"
    - path: "site/src/data/daylight.ts"
      provides: "daylightHours (suncalc, polar-aware)"
      contains: "alwaysUp"
    - path: "site/src/data/daylight.test.ts"
      provides: "solstice-edge unit tests (no NaN)"
    - path: "site/tests/e2e/panel.spec.ts"
      provides: "Playwright panel E2E skeleton"
  key_links:
    - from: "packages/domain/src/distribution.ts"
      to: "packages/domain/src/index.ts"
      via: "named re-export"
      pattern: "export \\{ percentile, perDoyDistribution \\}"
    - from: "site/src/data/daylight.ts"
      to: "suncalc"
      via: "getTimes import"
      pattern: "from \"suncalc\""
---

<objective>
Wave-0 foundation for the station chart panel: the two pure, unit-tested helpers that produce every number the panel draws, the two new runtime deps, and the failing E2E + build-size gates the panel plans turn green.

Purpose: The distribution math and daylight astronomy are the load-bearing new code (RESEARCH GATE = GREEN: distributions compute client-side from existing derived data, NO Phase 2 pipeline change). Building and pinning them TDD-first, in isolation, means Plans 02/03 only wire proven functions into DOM/ECharts.
Output: `perDoyDistribution` + `percentile` in `@betravedur/domain`; `daylightHours` in the site data layer; echarts/suncalc installed; `panel.spec.ts` E2E skeleton; a build-size chunk-split assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-station-chart-panel/06-RESEARCH.md
@.planning/phases/06-station-chart-panel/06-UI-SPEC.md

<interfaces>
<!-- Extracted from the codebase — executor uses these directly, no exploration needed. -->

From packages/domain/src/types.ts (DailyObservation — the decoded row shape):
```typescript
export interface DailyObservation {
  station: number;
  date: string;      // "YYYY-MM-DD"
  doy: number;       // leap-folded 1-365
  t: number | null;  // mean temp °C
  tx: number | null; tn: number | null;
  f: number | null;  // mean wind m/s
  fx: number | null; fg: number | null;
  dv: number | null; // wind dir deg (null on SYNOP)
  r: number | null;  // precip mm (null on AWS; missing NEVER coerced to 0)
}
export interface WindowSpec { startDoy: number; endDoy: number; } // may wrap (endDoy < startDoy)
export interface StationMeta { station: number; name: string; type: StationType; lat: number; lon: number; ele: number; start: number; ending: number | null; owner: string; }
```

From packages/domain/src/window.ts + coverage.ts (REUSE VERBATIM — already unit-tested, WR-03 wrap-correct):
```typescript
export function expandWindow(spec: WindowSpec): Set<number>;   // insertion order = window order (startDoy..365 then 1..endDoy)
export function groupBySeasonYear(rows: DailyObservation[], spec: WindowSpec): Map<number, DailyObservation[]>;
export function qualifyingYears(rowsByYear: Map<number, DailyObservation[]>, windowDays: Set<number>, metric: (o: DailyObservation) => number | null, minCoverage?: number /* =0.8 */): number[]; // sorted asc
export function effectiveN(qualifying: number[]): { n: number; sufficient: boolean }; // sufficient = n >= 3
```

From site/src/data/averages.ts (the PROVEN reshape pattern to mirror — decode → window → group → yearRange filter → qualify):
```typescript
import { decodeDerived, type DerivedFile } from "@betravedur/pipeline/derive";
export interface YearRange { from: number; til: number; }
// pattern (averages.ts:97-116):
//   const rows = decodeDerived(file);
//   const windowDays = expandWindow(window);
//   const allYears = groupBySeasonYear(rows, window);
//   const byYear = yearRange ? new Map([...allYears].filter(([y]) => y >= yearRange.from && y <= yearRange.til)) : allYears;
//   const qYears = qualifyingYears(byYear, windowDays, (o) => o.t);
//   const { n, sufficient } = effectiveN(qYears);
```

From packages/domain/src/index.ts (the ONLY public re-export surface — IN-01; a helper ships only when added here):
```typescript
export { leapFoldedDoy, expandWindow, groupBySeasonYear } from "./window.js";
export { qualifyingYears, effectiveN } from "./coverage.js";
// ... add distribution exports here
```
</interfaces>

<test_infra>
- Domain unit tests live in `packages/domain/test/*.test.ts` and import source via `../src/<name>.js`. Run: `npx vitest run distribution`.
- Site unit tests are co-located (`site/src/**/*.test.ts`, per root vitest.config.ts). Run: `npx vitest run daylight`.
- E2E: `site/tests/e2e/*.spec.ts`, Playwright drives the production preview build (`npm run build && npm run preview -- --port 4173`). Base URL `http://localhost:4173/betravedur/`. Existing specs: shell, markers, selection, score. Run: `npm run e2e -w site`.
- Existing marker pill selector (how a station is clicked): `#marker-overlay [data-station]`. Ranked panel root: `.ranked-list[aria-label="Bestu staðir"]`; rows: `.ranked-list__ol li[data-station]`.
</test_infra>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: percentile + perDoyDistribution pure domain helper (TDD)</name>
  <files>packages/domain/src/distribution.ts, packages/domain/src/index.ts, packages/domain/test/distribution.test.ts</files>
  <read_first>
    - packages/domain/src/window.ts (expandWindow insertion order, groupBySeasonYear signature)
    - packages/domain/src/coverage.ts (qualifyingYears + effectiveN — reuse verbatim)
    - packages/domain/src/index.ts (re-export surface to extend)
    - site/src/data/averages.ts lines 91-142 (the proven decode→window→group→yearRange→qualify reshape to mirror)
    - packages/domain/test/coverage.test.ts (test style: import from "../src/*.js", synthetic DailyObservation fixtures)
  </read_first>
  <behavior>
    - percentile(sorted, p) type-7 linear interpolation: single-element array → that element; p=0 → min; p=1 → max; p=0.5 on [1,2,3,4] → 2.5; rank interpolates between floor/ceil order statistics. Assumes ascending, non-empty input (document the contract).
    - perDoyDistribution(file, window, yearRange, metricSelector) returns { sufficient:false } when effectiveN over the qualifying years is < 3 (CHART-04 gate — SAME rule as computeMarkerDatum: qualifyingYears(...,0.8) + effectiveN N>=3).
    - When sufficient: returns { sufficient:true, n, perDoy } where perDoy is an array in `[...expandWindow(window)]` ORDER (wrap-correct: a Dec→Jan window plots Dec before Jan, NOT numeric 1..365).
    - Each perDoy entry: a doy with >=1 qualifying value → { doy, min, max, p10, p50, p90 }; a doy with zero qualifying values → { doy, missing:true } (explicit gap, NEVER a zero box).
    - Values bucketed per doy across QUALIFYING years only (mirror computeMarkerDatum: pool in-window days of qualifying years), skipping null metric values.
    - Precip variant: a separate `perDoyPrecip(file, window, yearRange)` (or a mode) returning per-doy MEDIAN total of `o.r` across qualifying years (research A2 / Open-Q-1 decision: MEDIAN, robust to one wet year), with { missing:true } for doys lacking qualifying rain OR when the `r` column is absent (AWS "án úrkomu") — never a zero. Document median choice in a comment + pin in a test.
    - Uses the `@betravedur/pipeline/derive` SUBPATH decodeDerived (never the root barrel — pulls Node built-ins). NO fetch, NO pipeline change.
  </behavior>
  <action>
    Create `packages/domain/src/distribution.ts` exporting `percentile(sorted: number[], p: number): number` (type-7, per RESEARCH Code Examples) and `perDoyDistribution(file, window, yearRange, selector)` plus the precip-median variant. Reshape by mirroring the averages.ts pattern in the interfaces block: decodeDerived → expandWindow → groupBySeasonYear → filter byYear to [yearRange.from..til] → qualifyingYears(...,0.8) → effectiveN. Bucket per doy across qualifying years, emit the 5-number summary per doy in expandWindow insertion order; emit { missing:true } for empty buckets. Define and export the result types (`PerDoyBox`, `PerDoyBar`, `DistributionResult`). Add all new exports to `packages/domain/src/index.ts` (named re-exports only — IN-01). Percentile method = type-7 linear; document it in the function doc so tests pin it. NOTE: distribution.ts imports decodeDerived from `@betravedur/pipeline/derive` — confirm the domain package may depend on the pipeline derive subpath; if a dependency cycle is a concern, place perDoyDistribution in `site/src/data/distribution.ts` instead and keep only `percentile` (pure, zero-dep) in the domain — Claude's discretion, but keep percentile in the domain either way and unit-test both. Write the tests FIRST (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>npx vitest run distribution</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function percentile" packages/domain/src/distribution.ts` (or site/src/data/distribution.ts if relocated)
    - `grep -q "export function perDoyDistribution" packages/domain/src/distribution.ts` (or relocated file)
    - `grep -Eq "percentile|perDoyDistribution" packages/domain/src/index.ts` when placed in domain
    - distribution.test.ts asserts: percentile([5],0.5)===5; percentile([1,2,3,4],0.5)===2.5; percentile(x,0)===min; percentile(x,1)===max
    - a test with < 3 qualifying years returns { sufficient:false }
    - a test with a doy lacking qualifying values yields an entry with `missing:true` (asserted; NOT a 0-valued box)
    - a wrapping window test asserts perDoy[0].doy is the window START doy (not numeric 1)
    - a precip test asserts the MEDIAN total is used and a rain-less/absent-`r` doy is `missing:true`, never 0
    - `npx vitest run distribution` exits 0
  </acceptance_criteria>
  <done>percentile + perDoyDistribution (+ precip median variant) implemented and unit-green, exported from the domain barrel (or site data layer), covering CHART-01/02/04 math with boundary, qualifying-years, wrap-order, and missing-gap cases.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Install echarts + suncalc; daylightHours polar-aware helper (TDD)</name>
  <files>site/package.json, site/package-lock.json, site/src/data/daylight.ts, site/src/data/daylight.test.ts</files>
  <read_first>
    - .planning/phases/06-station-chart-panel/06-RESEARCH.md "Daylight for the window (polar-safe)" code example + Pitfall 2 (polar-edge NaN) + Package Legitimacy Audit
    - packages/domain/src/types.ts (StationMeta.lat / .lon)
    - site/package.json (dependencies block — echarts/suncalc are the FIRST runtime deps beyond map libs)
  </read_first>
  <behavior>
    - daylightHours(date, lat, lon) returns a tagged union: { kind:"hours", hours } | { kind:"polar-day" } | { kind:"polar-night" }.
    - Branch on suncalc times.alwaysUp FIRST → polar-day; times.alwaysDown → polar-night; defensively treat null sunrise/sunset as polar-day. Only then compute (sunset - sunrise)/3_600_000.
    - Iceland summer solstice (e.g. 2020-06-21, lat 65, lon -18) → NEVER NaN (either a large hours value ~21-24 or kind:"polar-day"); winter solstice (2020-12-21) → NEVER NaN (small hours or kind:"polar-night").
    - A normal spring/autumn date at Iceland latitude → kind:"hours" with a finite 0 < hours < 24.
  </behavior>
  <action>
    Run `cd site && npm install echarts@6.1.0 suncalc@2.0.1` (pinned exact, both into `dependencies`). Do NOT install `@types/echarts` or `@types/suncalc` (both deprecated stubs; the packages ship their own types). Confirm no postinstall side-effects: `npm view echarts scripts.postinstall` and `npm view suncalc scripts.postinstall` both empty (legitimacy verified in RESEARCH Package Legitimacy Audit — slopcheck [OK]; this is a confirm step, NOT a human checkpoint). Verify package-lock.json pins 6.1.0 / 2.0.1. Then create `site/src/data/daylight.ts` exporting `daylightHours` per the behavior + RESEARCH code example (branch on alwaysUp/alwaysDown FIRST). Write `site/src/data/daylight.test.ts` FIRST (RED) with the solstice edges, then implement (GREEN). This helper is data-independent (pure lat/lon+doy) so the daylight readout renders even for a data-less station.
  </action>
  <verify>
    <automated>npx vitest run daylight</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"echarts": "6.1.0"' site/package.json` AND `grep -q '"suncalc": "2.0.1"' site/package.json`
    - `grep -Fq "6.1.0" site/package-lock.json` and suncalc 2.0.1 present in lockfile
    - NO `@types/echarts` / `@types/suncalc` in site/package.json
    - `grep -q "alwaysUp" site/src/data/daylight.ts` AND `grep -q 'from "suncalc"' site/src/data/daylight.ts`
    - daylight.test.ts asserts June-solstice and Dec-solstice at Iceland lat produce NO NaN (Number.isNaN(hours) is false OR kind is polar-day/polar-night)
    - `npx vitest run daylight` exits 0
  </acceptance_criteria>
  <done>echarts 6.1.0 + suncalc 2.0.1 pinned in site deps with no postinstall; daylightHours implemented, polar-branch-first, unit-green at Iceland solstice edges (CHART-03).</done>
</task>

<task type="auto">
  <name>Task 3: Playwright panel.spec skeleton + build-size chunk-split gate</name>
  <files>site/tests/e2e/panel.spec.ts</files>
  <read_first>
    - .planning/phases/06-station-chart-panel/06-UI-SPEC.md "Acceptance-Checkable Visual Criteria" (the 14 criteria) + "ECharts Loading Affordance"
    - site/tests/e2e/score.spec.ts (existing spec style: preview build, PILL selector, ranked panel selectors, page.on('pageerror'), test.fixme scaffolds)
    - site/playwright.config.ts (preview harness, baseURL /betravedur/)
    - .planning/phases/06-station-chart-panel/06-RESEARCH.md Pattern 3 + Pitfall 6 (lazy chunk / echarts not in entry bundle) + Assumption A4 (bundle-size must be build-verified)
  </read_first>
  <action>
    Create `site/tests/e2e/panel.spec.ts` as the E2E skeleton for the phase, mirroring score.spec.ts conventions (production preview build, `#marker-overlay [data-station]` pill click to open, `page.on('pageerror')` guard). Encode ALL 14 UI-SPEC acceptance criteria as `test.fixme(...)` placeholders (Plans 02/03 turn them green), with the exact selectors/asserts documented inline so the executor of later plans has no ambiguity: (1) click station → `section.station-panel[aria-label]` appears; (2) three titled chart canvases (Hiti/Vindur/Úrkoma figures each with a `<canvas>`); (3) three reading-key sentences as DOM text (`8 af hverjum 10`, `hægasta og hvassasta`, `eyða þýðir að úrkoma var ekki mæld`); (4) `Dagsbirta` label + `/\d+,\d+\s*klst\./`; (5) no-data station shows `engin gögn fyrir þetta tímabil` text, no populated canvas; (6) án-úrkomu station: temp+wind canvases present, precip shows `engin úrkomumæling á þessari stöð`; (7) close (`[aria-label="Loka"]`) removes panel, clears URL `st`, restores `.ranked-list[aria-label="Bestu staðir"]` visibility; (8) ranked list hidden while panel open; (9) Escape closes; (10) opening panel fires ZERO `data/` network requests (allow the one dynamic echarts JS chunk request); (11) grep-gate distribution semantics (no `--accent`/`--score-` in the chart option module, `--chart-temp/--chart-wind/--chart-precip` used); (12) reduced-motion → ECharts `animation:false`; (13) all panel numbers use comma decimal; (14) each figure exposes a text/aria-label distribution summary. Also add ONE non-fixme active test that FAILS meaningfully now (e.g. asserts the panel spec harness boots the preview and no pageerror — mirror score.spec's wave-0 smoke). Additionally create the build-size gate: add an npm script or a test (`site/tests/e2e/` or a small node script referenced in the plan) that runs `npm run build` and asserts (a) a distinct `chartPanel-*.js` chunk exists in `dist/assets` and (b) the entry/main chunk does NOT contain echarts (grep the entry chunk for an echarts marker string is absent). Document the exact assertion so Plan 03 (which creates chartPanel.ts) makes it pass; leave it as `test.fixme` if the chunk cannot exist until Plan 03, but write the assertion body fully.
  </action>
  <verify>
    <automated>cd site && npx playwright test tests/e2e/panel.spec.ts --project=chromium --list</automated>
  </verify>
  <acceptance_criteria>
    - `test -f site/tests/e2e/panel.spec.ts`
    - `grep -c "test.fixme\|test(" site/tests/e2e/panel.spec.ts` shows >= 14 criteria scaffolded (count via `grep -v '^//' | grep -c`)
    - The three exact reading-key substrings appear in the spec: `grep -q "8 af hverjum 10" site/tests/e2e/panel.spec.ts` and `grep -q "eyða þýðir að úrkoma var ekki mæld" site/tests/e2e/panel.spec.ts`
    - A build-size chunk-split assertion body exists referencing `chartPanel` and asserting echarts absence from the entry chunk: `grep -q "chartPanel" site/tests/e2e/panel.spec.ts` (or the build-gate file)
    - `npx playwright test tests/e2e/panel.spec.ts --list` enumerates the tests without error
    - Existing suites still green: `npm run e2e -w site` (shell/markers/selection/score unaffected)
  </acceptance_criteria>
  <done>panel.spec.ts scaffolds all 14 UI-SPEC criteria as fixme placeholders with exact selectors + one active harness-smoke test, and a build-size gate asserting echarts stays out of the entry chunk while a lazy chartPanel chunk appears (A4). Plans 02/03 flip these green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | New runtime deps (echarts, suncalc) enter the supply chain |
| derived file → domain helper | Already-loaded numeric data reshaped client-side (no new fetch) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | echarts/suncalc install | mitigate | Pin exact 6.1.0 / 2.0.1; assert no `scripts.postinstall`; slopcheck [OK] recorded in RESEARCH; lockfile committed |
| T-06-02 | DoS | perDoyDistribution over malformed/empty file | mitigate | Reuse defensive decode; below-gate → { sufficient:false } (no throw); missing bucket → explicit gap, never crash |
| T-06-03 | Tampering | polar-edge NaN in daylight | mitigate | Branch on alwaysUp/alwaysDown FIRST + null-time guard before subtraction (no NaN escapes to DOM) |
| T-06-SC | Tampering | npm installs | mitigate | slopcheck + confirm-no-postinstall step (research verified [OK]; not a human checkpoint per no-review directive) |
</threat_model>

<verification>
- `npx vitest run distribution` and `npx vitest run daylight` both green.
- `npx tsc --noEmit -p site` stays 0 errors.
- echarts 6.1.0 + suncalc 2.0.1 pinned in site/package.json + lockfile; no postinstall.
- `npm run e2e -w site` green (no prior-phase regression; panel.spec fixmes are pending, harness smoke passes).
</verification>

<success_criteria>
- percentile + perDoyDistribution (+ precip median variant) exported and unit-tested (boundaries, qualifying-years gate, wrap order, missing gap).
- daylightHours polar-safe and unit-tested at Iceland solstices.
- Two runtime deps installed, pinned, postinstall-free.
- panel.spec.ts scaffolds all 14 UI-SPEC criteria + a build-size chunk-split gate.
</success_criteria>

<output>
Create `.planning/phases/06-station-chart-panel/06-01-SUMMARY.md` when done.
</output>
