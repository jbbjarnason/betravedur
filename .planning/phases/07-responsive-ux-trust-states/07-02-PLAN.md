---
phase: 07-responsive-ux-trust-states
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - site/src/ui/infoPanel.ts
  - site/src/styles/trust.css
  - site/src/styles/controls.css
  - site/src/main.ts
  - site/tests/e2e/info.spec.ts
  - site/tests/e2e/shell.spec.ts
autonomous: true
requirements: [UX-04]
user_setup: []

must_haves:
  truths:
    - "A persistent top-right `i` button (aria-label `Um kortið`) opens a native <dialog> info panel at both 1280px and 390px"
    - "The info panel contains the prominent lead `Þetta er sögulegt meðaltal, ekki spá.`, the ATTRIBUTION credit (CC BY 4.0 + OpenStreetMap + Protomaps + Veðurstofa, from the domain constant), and an `uppfært {Icelandic date}` freshness line"
    - "On a bare first visit (no URL params, cleared localStorage) the panel auto-opens once; after dismiss, localStorage bv:info-dismissed==='1' and a reload does NOT auto-open, while the i button still opens it"
    - "A permalink URL (selection/st params present) does NOT auto-open a blocking modal — the restored view is present/interactive on load"
    - "The freshness line reflects the manifest max(lastFetched) (not a hardcoded literal); if unavailable the line is omitted (never Invalid Date)"
    - "The three legacy attribution hacks in controls.css (.panel-open margin-right:344px, max-width:60vw cap, --bar-height lift) are removed and replaced by the harmonized --attrib-safe-bottom rule; the CC BY 4.0 + OSM credit stays non-occluded at 1280 AND 390"
  artifacts:
    - path: "site/src/ui/infoPanel.ts"
      provides: "mountInfoPanel — i button, native <dialog> trust panel, first-visit localStorage, ATTRIBUTION + freshness"
      exports: ["mountInfoPanel"]
    - path: "site/src/styles/trust.css"
      provides: "info button + info-panel <dialog> styling + --attrib-safe-bottom attribution safe-zone rule"
      contains: "--attrib-safe-bottom"
  key_links:
    - from: "site/src/main.ts"
      to: "site/src/ui/infoPanel.ts mountInfoPanel"
      via: "mounted in boot(); receives freshness date from newestDataDate(manifest)"
      pattern: "mountInfoPanel"
    - from: "site/src/ui/infoPanel.ts"
      to: "@betravedur/domain ATTRIBUTION"
      via: "info-panel attribution built from the constant (never hardcoded)"
      pattern: "ATTRIBUTION"
    - from: "site/src/styles/controls.css"
      to: "MapLibre bottom controls"
      via: "--attrib-safe-bottom margin-bottom (replaces --bar-height + .panel-open + 60vw hacks)"
      pattern: "attrib-safe-bottom"
---

<objective>
The info/trust panel + attribution-solve-once slice (UX-04 + the recurring attribution debt). Add the persistent top-right `i` button and a native `<dialog>` panel that frames the data as historical-not-forecast (`Þetta er sögulegt meðaltal, ekki spá.`), carries the full CC BY 4.0 credit from the domain ATTRIBUTION constant, and shows an Icelandic `uppfært {date}` freshness line derived from the manifest. First-visit auto-opens once (localStorage flag), non-blocking for permalink-restored views. Simultaneously replace the three incremental attribution hacks in controls.css with one coherent `--attrib-safe-bottom` safe-zone rule + the always-legible info-panel credit backstop, and re-assert non-occlusion at both viewports.

Purpose: Prevent the #1 documented misconception (forecast vs history), satisfy the CC BY 4.0 / OSM licensing requirement robustly in every state, and surface data freshness — closing UX-04 and the attribution debt in one coherent change.

Output: `infoPanel.ts` (button + dialog + first-visit + freshness), harmonized attribution rules in `trust.css`/`controls.css`, activated `info.spec` criteria 6-9/18, and extended `shell.spec` non-occlusion asserts at 390 + info-panel-open.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-responsive-ux-trust-states/07-UI-SPEC.md
@.planning/phases/07-responsive-ux-trust-states/07-RESEARCH.md
@.planning/phases/07-responsive-ux-trust-states/07-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (Wave 1) — the freshness helper this plan consumes. -->
From site/src/data/freshness.ts:
```typescript
export function newestDataDate(manifest: Manifest): string | null;      // max(lastFetched) ISO or null
export function formatIcelandicDate(iso: string): string | null;        // "20. júlí 2026" or null
```

From packages/domain (the single-source-of-truth attribution constant — NEVER hardcode this text):
```typescript
export const ATTRIBUTION: {
  license: "CC BY 4.0";
  text_is: string;            // "Uppruni gagna: Veðurstofa Íslands. Veðurgögn gefin út …"
  text_en: string;
  sourceUrl: "https://athuganir.vedur.is/disclaimer";
  modifiedNotice_is: string;  // "Gögnunum hefur verið breytt af Betra Veður …"
};
```

From site/src/ui/attribution.ts (the basemap credit already composed for the MapLibre control — mirror the OSM/Protomaps portion in the info panel):
```typescript
export function attributionHtml(): string; // ATTRIBUTION.* + "© OpenStreetMap contributors · Protomaps"
```

From site/src/main.ts (boot seams — mount the info button in boot(); pass freshness after manifest loads):
```typescript
// boot(): renderHeader; showLoading(); const map = initMap(mapMount); mountLegend(document.body); (window).__map=map; wireMarkers(map);
// wireMarkers.install(): const {entries, muted, manifest} = await loadStationFiles(); ...
```

From site/src/styles/controls.css (the THREE hacks to DELETE — verbatim, lines ~267-296):
```css
.maplibregl-ctrl-bottom-right,
.maplibregl-ctrl-bottom-left { margin-bottom: calc(var(--bar-height, 100px) + var(--space-lg)); }
.maplibregl-ctrl-bottom-right .maplibregl-ctrl-attrib { max-width: 60vw; margin-left: auto; text-align: right; }
.maplibregl-ctrl-bottom-right .maplibregl-ctrl-attrib-inner { text-align: right; }
.panel-open .maplibregl-ctrl-bottom-right { margin-right: 344px; }
```
</interfaces>

<info_panel_copy>
<!-- UI-SPEC §Info panel content structure — EXACT final Icelandic strings (verbatim, do not alter). -->
- Title: `Um kortið`
- Trust lead (prominent, 14px 600): `Þetta er sögulegt meðaltal, ekki spá.`
- What it shows: `Kortið sýnir hvar á landinu veðrið hefur að jafnaði verið best fyrir valið tímabil ársins, reiknað úr raunverulegum mælingum Veðurstofunnar yfir valin ár — ekki veðurspá.`
- How to read it: `Grænni stöðvar hafa fengið hærri einkunn fyrir tímabilið. Smelltu á stöð til að sjá dreifingu hita, vinds og úrkomu. Veldu tímabil og árabil neðst; „meðaltal N ára“ sýnir á hve mörgum árum meðaltalið byggir.`
- Attribution: from `ATTRIBUTION.text_is` + `ATTRIBUTION.modifiedNotice_is` + a linked `CC BY 4.0` (href = ATTRIBUTION.sourceUrl) + `© OpenStreetMap contributors · Protomaps`
- Freshness: `uppfært {date}` (Icelandic date; omit the whole line if freshness is null)
- Close a11y label: `Loka`
- Info button a11y label: `Um kortið`
- localStorage key: `bv:info-dismissed` (set to `"1"` on dismiss)
</info_panel_copy>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: infoPanel.ts — i button, native <dialog> trust panel, first-visit localStorage, ATTRIBUTION + freshness</name>
  <files>site/src/ui/infoPanel.ts, site/src/styles/trust.css</files>
  <read_first>
    - site/src/ui/attribution.ts (how ATTRIBUTION is composed — mirror text_is + modifiedNotice_is + linked CC BY 4.0 + OSM/Protomaps, but built via DOM nodes, NOT string-HTML)
    - packages/domain/src/attribution.ts (the ATTRIBUTION constant fields)
    - site/src/ui/stationPanel.ts lines 119-140 (buildCloseGlyph / SVGNS — inline-SVG-via-DOM pattern to reuse for the i glyph)
    - site/src/data/freshness.ts (newestDataDate/formatIcelandicDate — Plan 01)
    - .planning/phases/07-responsive-ux-trust-states/07-UI-SPEC.md §Info panel + §First-visit auto-open + §Data freshness + §Accessibility (dialog focus/Escape)
  </read_first>
  <behavior>
    - `mountInfoPanel(parent: HTMLElement, opts: { freshnessDate: string | null }): void`.
    - Renders a persistent `<button class="info-button" aria-label="Um kortið">` fixed top-right (z-index 10) with an inline-SVG lowercase "i"-in-a-circle glyph (24x24 in a ≥44px hit target), built via createElementNS (never string-HTML). Ink stroke; transparent → --dominant hover; 2px solid var(--ink) focus ring. Never accent.
    - Clicking the button calls `dialog.showModal()` on a native `<dialog class="info-panel" aria-labelledby="info-title">` whose sections are exactly the §Info panel copy in order: title → trust lead → what → how → attribution block → freshness line (omitted when freshnessDate is null) → close button (aria-label `Loka`).
    - Attribution is built from the domain `ATTRIBUTION` constant + the OSM/Protomaps credit as DOM nodes (a real `<a href={ATTRIBUTION.sourceUrl}>CC BY 4.0</a>` anchor). NEVER retype the Icelandic license text.
    - Freshness line text = `uppfært ${freshnessDate}` when non-null; when null, the line element is NOT appended.
    - First-visit auto-open: on mount, if `localStorage.getItem("bv:info-dismissed") !== "1"` AND `location.search` carries NO selection/st params (bare first visit), call `showModal()` once. When it auto-opens under a permalink it must NOT (permalink guard: skip auto-open entirely when params present). Dismiss (close button, Escape, backdrop) sets `localStorage["bv:info-dismissed"]="1"`.
    - Native <dialog> provides focus-trap + Escape + backdrop; on close, focus returns to the info button (native behavior + explicit `.focus()` fallback).
    - Test (Vitest, jsdom if available — else keep the DOM-buildable pieces pure): a `buildInfoDialog(freshnessDate)` pure helper returns the dialog element with the trust lead text present, the attribution containing `CC BY 4.0` and `OpenStreetMap`, and the freshness line present iff freshnessDate non-null. (If jsdom/localStorage is unavailable in the unit runtime, cover the pure DOM builder here and defer first-visit/permalink behavior to the E2E in Task 2.)
  </behavior>
  <action>
    Create `site/src/ui/infoPanel.ts` exporting `mountInfoPanel(parent, {freshnessDate})` and an exported pure `buildInfoDialog(freshnessDate: string | null): HTMLDialogElement` per the behavior block. Import `ATTRIBUTION` from `@betravedur/domain`. Build every node via createElement/createElementNS + textContent; the only markup is the real `<a>` anchors (href set via setAttribute). Reduced-motion: no entrance animation (CSS owns this; the dialog just showModal()s). Guard the permalink case by checking `location.search` for the selection/`st` params (any non-empty search that would restore a view — a simple `location.search.length > 1` check is sufficient and matches the "bare first visit only" rule; document the choice).
    Add to `site/src/styles/trust.css`: `.info-button` (fixed top-right, ≥44px hit target, transparent→--dominant hover, 2px ink focus ring, z-index 10, NEVER --accent) and `.info-panel` dialog styling (same glass surface rgba(255,255,255,0.92)+blur, --hairline border, section rhythm via --space-sm/--space-lg, the trust lead as 14px 600 ink with --space-sm breathing room, freshness/attribution as 11px muted). `::backdrop` a light wash. Under `prefers-reduced-motion` no animation. No --score-*/--chart-*/--accent references.
    Write `site/src/ui/infoPanel.test.ts` (Vitest) covering `buildInfoDialog` per the test in the behavior block.
  </action>
  <verify>
    <automated>cd site && npx vitest run infoPanel && grep -q "ATTRIBUTION" src/ui/infoPanel.ts && ! grep -Eq "sögulegt meðaltal, ekki spá" src/ui/attribution.ts && ! grep -Eq "\-\-accent|\-\-score-|\-\-chart-" src/styles/trust.css && npx tsc --noEmit -p .</automated>
  </verify>
  <done>buildInfoDialog unit test green (trust lead + CC BY 4.0 + OpenStreetMap + conditional freshness line); infoPanel.ts builds attribution from ATTRIBUTION (not hardcoded); trust.css has no accent/score/chart tokens; tsc 0 errors.</done>
</task>

<task type="auto">
  <name>Task 2: Wire info panel into boot + solve attribution once (delete hacks, add --attrib-safe-bottom) + activate E2E</name>
  <files>site/src/main.ts, site/src/styles/controls.css, site/src/styles/trust.css, site/tests/e2e/info.spec.ts, site/tests/e2e/shell.spec.ts</files>
  <read_first>
    - site/src/main.ts (boot() mount order + wireMarkers.install where manifest is available for the freshness date)
    - site/src/styles/controls.css lines 245-296 (the THREE hacks to DELETE — verbatim in <interfaces>)
    - site/src/ui/controlBar.ts lines 114-125 (reportBarHeight writes --bar-height — the safe-zone rule consumes the control-bar height; keep --bar-height being written, just re-target its consumer)
    - site/tests/e2e/shell.spec.ts lines 49-150 (existing attribution non-occlusion asserts to extend to 390 + info-panel-open)
    - .planning/phases/07-responsive-ux-trust-states/07-UI-SPEC.md §The Attribution Solution + Acceptance criteria 6-12, 18
    - .planning/phases/07-responsive-ux-trust-states/07-RESEARCH.md §Pattern 2 + Pitfall 6 (re-run non-occlusion at BOTH viewports before/after deleting)
  </read_first>
  <action>
    Wire `site/src/main.ts`: in `boot()`, mount the info button early (it is static chrome, no data dependency) so it is present before data loads — but the freshness date needs the manifest. Mount `mountInfoPanel(document.body, { freshnessDate: null })` in boot() for the button+first-visit, THEN once the manifest loads in `wireMarkers.install()`, compute `const iso = newestDataDate(manifest); const freshnessDate = iso ? formatIcelandicDate(iso) : null;` and update the panel's freshness line. Simplest coherent approach: expose a small updater from mountInfoPanel (return a handle `{ setFreshness(date: string|null): void }`), call it after the manifest resolves. Import `newestDataDate`, `formatIcelandicDate` from `./data/freshness.js`. (The first-visit auto-open can run in boot() before freshness resolves — the freshness line simply appears once set; auto-open must not wait on the network.)
    Solve attribution once in `site/src/styles/controls.css`: DELETE the three hack rules (the `margin-bottom: calc(var(--bar-height)…)` on the bottom controls, the `max-width:60vw`/`margin-left:auto`/`text-align:right` cap block, and the `.panel-open .maplibregl-ctrl-bottom-right { margin-right:344px }` rule). REPLACE with a single harmonized rule keyed on a new custom property `--attrib-safe-bottom`: `.maplibregl-ctrl-bottom-right, .maplibregl-ctrl-bottom-left { margin-bottom: calc(var(--attrib-safe-bottom, 0px) + var(--space-sm)); }`. Define `--attrib-safe-bottom` in trust.css (or :root) as the control-bar-height baseline: set it from the existing `--bar-height` write (`:root { --attrib-safe-bottom: var(--bar-height, 100px); }`) so the desktop control bar reserves the band. (Plan 03 will raise `--attrib-safe-bottom` to the sheet peek top on mobile — leave that seam clean; do NOT implement the sheet coupling here.) Remove the now-unused `.panel-open`-based attribution offset; the stationPanel.ts `panel-open` body-class may remain (it is harmless) but no attribution rule keys on it anymore. Keep the compact AttributionControl (init.ts) untouched — it owns its own (i) collapse.
    Add `.info-button` fixed positioning that clears the wordmark and (mobile) does not collide with future chips (top-right, within the 56px header band).
    Activate `site/tests/e2e/info.spec.ts` criteria 6-9, 18 (remove the fixmes): info button opens the dialog with the trust lead + CC BY 4.0 + OpenStreetMap + `uppfært` line at 1280 AND 390; first-visit auto-open then `bv:info-dismissed` suppression on reload (control localStorage via `page.addInitScript`/fresh context per research); permalink guard (goto a URL with `st`/selection params, assert no blocking auto-open and the restored view interactive); info-panel Escape closes + focus returns to the button.
    Extend/activate `site/tests/e2e/shell.spec.ts` (or the responsive.spec attribution fixmes) for criteria 10-12: attribution `(i)` credit not occluded by legend/bar/panel at 1280 with the station side-panel open, not hidden behind the sheet peek at 390, and grep-gate that `panel-open`/`60vw` are ABSENT from controls.css while `--attrib-safe-bottom` is present. Re-run the EXISTING non-occlusion asserts (they must still pass after deleting the hacks — Pitfall 6).
  </action>
  <verify>
    <automated>cd site && grep -q "attrib-safe-bottom" src/styles/controls.css && ! grep -Eq "60vw" src/styles/controls.css && ! grep -Eq "panel-open .maplibregl-ctrl" src/styles/controls.css && grep -q "mountInfoPanel" src/main.ts && npx tsc --noEmit -p . && npx playwright test info shell -x</automated>
  </verify>
  <done>info button + first-visit panel wired in boot() with freshness from the manifest; the three controls.css hacks deleted and replaced by --attrib-safe-bottom; info.spec criteria 6-9/18 active-green; shell.spec attribution non-occlusion green at 1280 + 390 + panel-open, with panel-open/60vw absent and --attrib-safe-bottom present; tsc 0 errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest → DOM (freshness) | Machine-formatted date crosses into the dialog |
| domain ATTRIBUTION → DOM | License text + a real anchor href cross into the dialog |
| localStorage → auto-open decision | A dismissed-hint boolean (not user data) gates a cosmetic auto-open |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering (XSS) | infoPanel dialog content | mitigate | All copy via textContent; only real `<a>` anchors with setAttribute href; ATTRIBUTION from the constant, freshness a formatted date — no raw manifest field as HTML |
| T-07-05 | Information disclosure (licensing) | attribution safe-zone refactor | mitigate | The full CC BY 4.0 + OSM + Protomaps credit stays reachable in the modal info panel (always legible) AND the compact map control; non-occlusion re-asserted at both viewports before finalizing (Pitfall 6) |
| T-07-06 | Tampering | localStorage bv:info-dismissed | accept | Only a boolean flag; no PII, no trust decision depends on it (auto-open is cosmetic) |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages this plan (audit: none) |
</threat_model>

<verification>
- `cd site && npx vitest run` green (infoPanel + prior).
- `cd site && npx tsc --noEmit -p .` → 0 errors.
- `cd site && npx playwright test info shell -x` green; full `npm run e2e -w site` green at wave close (no prior-phase regression).
- Grep gates: controls.css has `--attrib-safe-bottom`, and NO `60vw` / `panel-open .maplibregl-ctrl`; main.ts has `mountInfoPanel`; attribution.ts still does NOT contain the trust-lead copy (info copy lives in infoPanel.ts, license text in the domain constant).
</verification>

<success_criteria>
- UX-04 info panel: top-right i button opens a native <dialog> with the prominent `Þetta er sögulegt meðaltal, ekki spá.` lead, the ATTRIBUTION-sourced credit, and an Icelandic `uppfært {date}` freshness line (omitted when unavailable, never hardcoded).
- First-visit auto-opens once (localStorage `bv:info-dismissed`), non-blocking under a permalink; the button re-opens it for repeat visitors.
- Attribution solved once: the three controls.css hacks deleted, `--attrib-safe-bottom` harmonized rule in place, CC BY 4.0 + OSM non-occluded at 1280 + 390 + panel-open (re-asserted).
- No new runtime dependency; tsc 0 errors; full E2E green.
</success_criteria>

<output>
Create `.planning/phases/07-responsive-ux-trust-states/07-02-SUMMARY.md` when done.
</output>
