---
phase: 07-responsive-ux-trust-states
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/tests/e2e/responsive.spec.ts
  - site/tests/e2e/info.spec.ts
  - site/tests/e2e/states.spec.ts
  - site/src/data/freshness.ts
  - site/src/data/freshness.test.ts
  - site/src/ui/bottomSheet.ts
  - site/src/ui/bottomSheet.test.ts
  - site/src/ui/states.ts
  - site/src/styles/trust.css
  - site/src/map/init.ts
  - site/src/main.ts
autonomous: true
requirements: [UX-05, UX-04]
user_setup: []

must_haves:
  truths:
    - "On boot, a `hleð…` loading affordance is present over the map area before markers paint, and is removed once markers render"
    - "When the MapLibre style/PMTiles basemap fails to load, a text overlay shows `Ekki tókst að hlaða kortið` + `Reyndu að hlaða síðunni aftur.` with role=alert, and the header stays up"
    - "When stations.json is empty ([]) or 404s, a text overlay shows `Engar veðurstöðvar` over a rendered basemap (not a blank white screen)"
    - "freshness helper derives the newest data date from manifest max(lastFetched) client-side and formats it as an Icelandic human date; missing/empty input omits the line (never Invalid Date)"
    - "Wave 0 Playwright skeletons (responsive/info/states) and unit specs (freshness/bottomSheet) exist and run green (active smoke + test.fixme placeholders for later criteria)"
  artifacts:
    - path: "site/src/data/freshness.ts"
      provides: "newestDataDate + formatIcelandicDate pure helpers"
      exports: ["newestDataDate", "formatIcelandicDate"]
    - path: "site/src/ui/bottomSheet.ts"
      provides: "matchMedia(640px) signal + snapNearest pure helper (drag controller stub for Plan 03)"
      exports: ["MOBILE_QUERY", "snapNearest"]
    - path: "site/src/ui/states.ts"
      provides: "loading/map-error/empty-stations renderers + aria-live announcer"
      exports: ["showLoading", "hideLoading", "showMapError", "showEmptyState"]
    - path: "site/src/styles/trust.css"
      provides: "loading/error/empty overlay glass-surface presentational classes"
      contains: ".bv-state"
    - path: "site/tests/e2e/states.spec.ts"
      provides: "UX-05 map-error (route-abort pmtiles), empty stations (route-fulfill []), initial loading"
    - path: "site/tests/e2e/responsive.spec.ts"
      provides: "Wave-0 responsive skeleton at 1280/390"
    - path: "site/tests/e2e/info.spec.ts"
      provides: "Wave-0 info-panel skeleton"
  key_links:
    - from: "site/src/map/init.ts"
      to: "site/src/ui/states.ts showMapError"
      via: "map.on('error', ...) handler"
      pattern: "map\\.on\\(\\s*['\"]error['\"]"
    - from: "site/src/main.ts"
      to: "site/src/ui/states.ts"
      via: "showLoading before install / hideLoading after first renderForState / showEmptyState on empty or catch"
      pattern: "showLoading|hideLoading|showEmptyState"
    - from: "site/src/main.ts"
      to: "site/src/data/freshness.ts newestDataDate"
      via: "reads manifest, computes freshness date passed to info panel in Plan 02"
      pattern: "newestDataDate"
---

<objective>
The trust-states foundation slice: establish the Wave-0 test harness (Playwright specs at both viewports + unit specs) and the two pure helpers later plans consume (freshness date, bottom-sheet snap math + matchMedia signal), THEN deliver the three distinct UX-05 state seams — an initial loading affordance, a MapLibre map-load-error overlay, and an empty-stations overlay — wired at the exact `init.ts` / `main.ts` seams identified in research.

Purpose: Close the accumulated UX-05 debt (map-error + empty-stations silently console.error'd since Phase 3) and give Plans 02 and 03 a green test harness plus tested pure helpers. After this plan a real user sees a loading cue on boot, a legible error if the map fails, and a legible empty state if there are no stations — where before they saw a blank/silent screen.

Output: `freshness.ts` + `bottomSheet.ts` pure helpers (unit-tested), `states.ts` + `trust.css` state renderers, the three UX-05 seams wired into `init.ts`/`main.ts`, and five new test files (three E2E skeletons, two unit specs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-responsive-ux-trust-states/07-UI-SPEC.md
@.planning/phases/07-responsive-ux-trust-states/07-RESEARCH.md
@.planning/phases/07-responsive-ux-trust-states/07-VALIDATION.md

<interfaces>
<!-- Extracted from the codebase — executor uses these directly, no exploration needed. -->

From site/src/data/load.ts (the manifest shape the freshness helper reads):
```typescript
export interface ManifestEntry {
  file: string;
  hash?: string;
  from?: number;
  to?: number;
  lastFetched?: string; // ISO, e.g. "2026-07-20T07:18:37.625Z"
}
export interface Manifest {
  stations: Record<string, ManifestEntry>;
}
export async function loadStations(base: string): Promise<StationMeta[]>; // throws on non-ok HTTP
export async function loadManifest(base: string): Promise<Manifest>;      // throws on non-ok HTTP
```

From site/src/map/init.ts (where the map-error handler is added; attributionControl already compact):
```typescript
export function initMap(container: HTMLElement): maplibregl.Map;
// map is constructed with attributionControl:false then addControl(new AttributionControl({compact:true, ...}))
```

From site/src/main.ts (the boot seams — do NOT restructure the URL/recompute flow):
```typescript
// boot(): renderHeader(headerMount); const map = initMap(mapMount); mountLegend(document.body);
//         (window).__map = map; wireMarkers(map);
// wireMarkers(map).install(): const {entries, muted, manifest} = await loadStationFiles();
//   ... buildStationCache -> renderForState(map, cache, muted, initial) [FIRST PAINT] ...
//   catch (err) { console.error("[betravedur] marker load failed", err); }  // <-- Pitfall 4 seam
// loadStationFiles(): const [stations, manifest] = await Promise.all([loadStations(BASE), loadManifest(BASE)]);
```

Icelandic month array (research Code Examples — deterministic, avoids the is-IS ICU fallback the codebase already documents for NumberFormat in stationPanel.ts formatIce):
```typescript
const IS_MONTHS = ["janúar","febrúar","mars","apríl","maí","júní",
  "júlí","ágúst","september","október","nóvember","desember"];
```
</interfaces>

<existing_patterns>
- `formatIce` in stationPanel.ts owns the is-IS separator by hand (locale NumberFormat fell back to a dot in headless) — freshness date MUST follow the same discipline (hand-rolled month array, NOT Intl locale month names).
- No string-HTML injection: every DOM node via createElement + textContent (T-05/T-06 grep gate). State overlays are plain <div>/<p>.
- Existing E2E driving convention (score.spec.ts / panel.spec.ts): run against the PRODUCTION preview build, gate on `window.__map.isStyleLoaded()` + a `#marker-overlay [data-station]` pill, attach `page.on('pageerror')`. Multi-viewport via `page.setViewportSize({width,height})` (shell.spec.ts / selection.spec.ts). Desktop 1280x800, mobile 390x844.
- Failure simulation (research Validation): `page.route("**/*.pmtiles", r => r.abort())` for map-style failure; `page.route("**/data/stations.json", r => r.fulfill({status:200, contentType:"application/json", body:"[]"}))` for empty; `status:404` variant exercises the res.ok throw.
</existing_patterns>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freshness helper + bottom-sheet pure helpers + Wave-0 unit specs</name>
  <files>site/src/data/freshness.ts, site/src/data/freshness.test.ts, site/src/ui/bottomSheet.ts, site/src/ui/bottomSheet.test.ts</files>
  <read_first>
    - site/src/data/load.ts (Manifest / ManifestEntry shape — lastFetched is the freshness source)
    - site/src/ui/stationPanel.ts lines 96-102 (formatIce — the is-IS-separator-by-hand discipline to mirror)
    - .planning/phases/07-responsive-ux-trust-states/07-RESEARCH.md §Pattern 3 + Pitfall 3 (client-side max(lastFetched); NO manifest change; hand-rolled month array)
  </read_first>
  <behavior>
    freshness.ts — `newestDataDate(manifest: Manifest): string | null`:
    - Returns the MAX (lexicographically-latest ISO === chronologically-latest) `lastFetched` across `manifest.stations`, or `null` when there are no stations / no parseable lastFetched.
    - Test: manifest with two stations lastFetched 2026-07-20T07:18:37.625Z and 2026-07-19T...  → returns the 2026-07-20 ISO.
    - Test: empty stations {} → null. Test: entries all missing lastFetched → null. Test: a single malformed/empty lastFetched string → null (never a bogus date).
    freshness.ts — `formatIcelandicDate(iso: string): string | null`:
    - Parses the ISO; returns `"{d}. {IS_MONTHS[m]} {yyyy}"` using UTC getters + the hand-rolled 12-element month array (janúar…desember). e.g. "20. júlí 2026".
    - Test: "2026-07-20T07:18:37.625Z" → "20. júlí 2026". Test: "2026-01-05T00:00:00.000Z" → "5. janúar 2026".
    - Test: invalid/empty string → null (Number.isNaN(date) guard) — NEVER "Invalid Date".
    bottomSheet.ts — `MOBILE_QUERY = "(max-width: 640px)"` constant (the single 640px source of truth, identical to the CSS @media) + `snapNearest(currentY, peekY, expandedY): number`:
    - Pure: returns whichever of peekY/expandedY the currentY is numerically closer to (ties → expandedY, i.e. prefer the more-open snap).
    - Test: currentY nearer peekY → peekY. Test: nearer expandedY → expandedY. Test: exact midpoint → expandedY.
    (The pointer-drag CONTROLLER itself is a Plan 03 concern; this task ships only the constant + the pure snap math + a stub `export function attachSheet(...): () => void` that returns a no-op teardown, so Plan 03 fills the body. Keep the stub typed so tsc passes.)
  </behavior>
  <action>
    Create `site/src/data/freshness.ts` exporting `newestDataDate(manifest)` and `formatIcelandicDate(iso)` per the behavior block, importing the `Manifest` type from `../data/load.js`. Use UTC getters (getUTCDate/getUTCMonth/getUTCFullYear) and the hand-rolled `IS_MONTHS` array — do NOT use `Intl.DateTimeFormat("is-IS")` for month names (RESEARCH Pitfall 3: the codebase already documents is-IS ICU fallback for NumberFormat). Guard both functions defensively: `newestDataDate` returns null for empty/missing; `formatIcelandicDate` returns null when `Number.isNaN(new Date(iso).getTime())`.
    Create `site/src/ui/bottomSheet.ts` exporting `MOBILE_QUERY` (the literal `"(max-width: 640px)"`), `snapNearest(currentY, peekY, expandedY)` pure helper, and a typed stub `attachSheet(sheetEl: HTMLElement, handleEl: HTMLElement, opts: {peekY: number; expandedY: number; onSnap?: (y: number) => void}): () => void` that (for now) returns `() => {}` — Plan 03 implements the Pointer-Events drag body. Add a header comment naming the coupling: `MOBILE_QUERY must stay byte-identical to the @media (max-width:640px) breakpoint in controls.css/panel.css (RESEARCH Pitfall 2)`.
    Write `freshness.test.ts` and `bottomSheet.test.ts` (Vitest) covering every case in the behavior block.
  </action>
  <verify>
    <automated>cd site && npx vitest run freshness bottomSheet && npx tsc --noEmit -p .</automated>
  </verify>
  <done>freshness + bottomSheet unit specs green; tsc 0 errors; newestDataDate/formatIcelandicDate return null (never Invalid Date) on bad input; snapNearest ties resolve to expandedY.</done>
</task>

<task type="auto">
  <name>Task 2: UX-05 state renderers + trust.css + wire the three seams (loading / map-error / empty-stations)</name>
  <files>site/src/ui/states.ts, site/src/styles/trust.css, site/src/map/init.ts, site/src/main.ts</files>
  <read_first>
    - site/src/main.ts (boot + wireMarkers.install first-paint + catch seam; loadStationFiles)
    - site/src/map/init.ts (where map.on('error') goes; attributionControl already compact)
    - site/src/styles/panel.css lines 140-149 (the glass-surface + muted-ink no-data treatment to match)
    - site/src/styles/tokens.css lines 10-26 (space/color tokens: --dominant, --secondary, --ink, --muted-ink, --hairline)
    - .planning/phases/07-responsive-ux-trust-states/07-UI-SPEC.md §Loading/Error/Empty States + §Copywriting Contract (exact strings)
    - .planning/phases/07-responsive-ux-trust-states/07-RESEARCH.md Pitfalls 4 & 5 (two distinct seams) + Code Examples
  </read_first>
  <action>
    Create `site/src/ui/states.ts` — a small module owning ONE reused overlay host (z30) over the map area plus an aria-live region. Export:
      - `showLoading()`: renders `hleð…` (muted small) into the overlay host with `aria-live="polite"`. Idempotent.
      - `hideLoading()`: removes only the loading affordance (leaves an error/empty overlay if one is showing).
      - `showMapError(heading: string, body: string)`: renders heading (14px 600 ink) + body (14px 400 muted) as TEXT with `role="alert"` / `aria-live="assertive"`. Header + info button must stay up (do NOT clear #map or the header).
      - `showEmptyState(heading: string, body: string)`: renders heading + body as TEXT with `aria-live="polite"` over the (still-rendered) basemap.
    All copy is passed in by callers (main.ts/init.ts) from the UI-SPEC strings — states.ts holds NO hardcoded Icelandic beyond the `hleð…` loading token (Claude's discretion; keep it in a COPY const). Every node via createElement + textContent (no innerHTML). Only ONE overlay shows at a time (loading → then markers/empty/error): showMapError/showEmptyState first call hideLoading.
    Create `site/src/styles/trust.css` — one presentational class family (`.bv-state` + modifiers) for the overlay: centered over the map area, `rgba(255,255,255,0.92)` + `backdrop-filter: blur(8px)`, `--ink` heading / `--muted-ink` body, `1px solid var(--hairline)`, z-index 30, `--space-md` padding. Under `prefers-reduced-motion: reduce` no animation. NEVER references `--accent` or any `--score-*`/`--chart-*` token. Import trust.css in main.ts alongside the existing style imports.
    Wire `site/src/map/init.ts`: after constructing `map`, add `map.on("error", (e) => { console.error("[betravedur] map error", e?.error ?? e); showMapError("Ekki tókst að hlaða kortið", "Reyndu að hlaða síðunni aftur."); })` (UX-05 map-load-error, per D map-error decision). Import showMapError from `../ui/states.js`. This REPLACES the silent-failure path (Phase 3 debt). Keep the compact AttributionControl exactly as-is.
    Wire `site/src/main.ts`:
      1. In `boot()`, call `showLoading()` immediately after `renderHeader` (before initMap), so the loading affordance is present before markers paint.
      2. In `wireMarkers.install()`, immediately after the FIRST `renderForState(map, cache, muted, initial)` (first paint), call `hideLoading()`.
      3. In `loadStationFiles()` (or in install before building the cache): if `stations.length === 0`, call `showEmptyState("Engar veðurstöðvar", "Engar veðurstöðvar fundust til að sýna á kortinu.")` and return early so no empty marker layer paints. (The basemap still renders — do not touch the map.)
      4. In the `install()` `catch (err)` block (Pitfall 4): keep the `console.error`, AND surface a visible state — if the failure is the stations fetch (empty/404), the empty state; otherwise the map-error copy is acceptable as the generic load-failure. Choose `showEmptyState("Engar veðurstöðvar", …)` for the catch as the default data-load-failure affordance (do NOT leave it silent). Keep this defensive: never re-throw, never white-screen.
    Do NOT alter the store/URL/recompute/viewport wiring — only add the state calls at the named points.
  </action>
  <verify>
    <automated>cd site && grep -q "map.on(\"error\"" src/map/init.ts && grep -Eq "showLoading|hideLoading|showEmptyState" src/main.ts && grep -q "bv-state" src/styles/trust.css && ! grep -Eq "\-\-accent|\-\-score-|\-\-chart-" src/styles/trust.css && npx tsc --noEmit -p .</automated>
  </verify>
  <done>init.ts registers map.on("error") → showMapError with the exact Icelandic copy; main.ts shows loading before paint, hides after first render, and surfaces empty-stations on []/404/catch; trust.css defines .bv-state with no accent/score/chart tokens; tsc 0 errors.</done>
</task>

<task type="auto">
  <name>Task 3: Wave-0 E2E skeletons (states active + responsive/info fixmes)</name>
  <files>site/tests/e2e/states.spec.ts, site/tests/e2e/responsive.spec.ts, site/tests/e2e/info.spec.ts</files>
  <read_first>
    - site/tests/e2e/panel.spec.ts (the Wave-0 skeleton pattern: active smoke + test.fixme placeholders, waitForMarkers, pageerror guard)
    - site/tests/e2e/shell.spec.ts lines 49-150 (attribution non-occlusion asserts + setViewportSize pattern to reuse/extend)
    - site/playwright.config.ts (preview-build webServer, baseURL /betravedur/)
    - .planning/phases/07-responsive-ux-trust-states/07-UI-SPEC.md §Acceptance-Checkable Visual Criteria (all 19 — encode exact selectors in fixmes)
    - .planning/phases/07-responsive-ux-trust-states/07-RESEARCH.md §Validation (route-abort/fulfill failure simulation)
  </read_first>
  <action>
    Create `site/tests/e2e/states.spec.ts` with ACTIVE tests for UX-05 (criteria 13/14/15):
      - Initial loading (13): intercept-delay markers if needed, assert a `.bv-state` `hleð…` affordance is present early and gone after `#marker-overlay [data-station]` renders. If timing is hard to catch deterministically on the fast preview build, assert the loading element mounts on `showLoading` via a boot-time check and is removed post-paint (a `page.waitForFunction` on absence after markers). Keep it deterministic; a `test.fixme` is acceptable ONLY if genuinely unobservable — prefer active.
      - Map-load error (14): `await page.route("**/*.pmtiles", r => r.abort())` BEFORE `goto`, then assert a `[role="alert"]` overlay contains `Ekki tókst að hlaða kortið` and `Reyndu að hlaða síðunni aftur.`, and the `<header>` wordmark `Betra Veður` is still present.
      - Empty stations (15): `await page.route("**/data/stations.json", r => r.fulfill({status:200, contentType:"application/json", body:"[]"}))` before goto, assert the overlay text `Engar veðurstöðvar` is present AND `canvas.maplibregl-canvas` is visible (basemap rendered, not blank). Add a 404 variant asserting the same empty affordance via the catch path.
    Create `site/tests/e2e/responsive.spec.ts` — an ACTIVE smoke (preview boots, markers render at 1280 with a pageerror guard, mirroring panel.spec) + `test.fixme` placeholders (with exact selectors) for criteria 1-5, 10-12 (Plan 03 + Plan 02 activate these): mobile-sheet-vs-side-panel, map-pannable-with-sheet, no-horizontal-overflow, touch-targets≥44px, chips, attribution-not-occluded desktop+mobile, no-legacy-hacks grep. Use `PILL = "#marker-overlay [data-station]"`, `PANEL = "section.station-panel[aria-label]"`.
    Create `site/tests/e2e/info.spec.ts` — an ACTIVE smoke + `test.fixme` placeholders (exact selectors) for criteria 6-9, 18 (Plan 02 activates): info-button opens panel with `Þetta er sögulegt meðaltal, ekki spá.`, attribution contains `CC BY 4.0` + `OpenStreetMap`, `uppfært` line, first-visit auto-open + localStorage `bv:info-dismissed` suppression, permalink guard, info-panel Escape/focus. Use `INFO_BTN = 'button.info-button[aria-label="Um kortið"]'`.
    Every spec attaches `page.on("pageerror", ...)` and fails on an uncaught error (Phase 3-6 convention).
  </action>
  <verify>
    <automated>cd site && npx playwright test states responsive info -x</automated>
  </verify>
  <done>states.spec active tests green (loading present-then-gone, map-error alert with exact copy + header up, empty-stations text over a rendered basemap incl. 404 variant); responsive.spec + info.spec boot-smoke green with fixme placeholders naming every remaining criterion + exact selectors; no pageerror in any spec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network → client (fetch stations.json/manifest.json/pmtiles) | Untrusted transport: 404/empty/malformed responses cross here; failures must degrade to legible text, never a blank screen or a leaked internal error string |
| manifest data → DOM (freshness date) | A machine-formatted date derived from manifest ISO strings crosses into the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information disclosure | states.ts error copy | mitigate | Show FIXED Icelandic copy only; log the raw error to console (V7) — never render `e.error` into the overlay |
| T-07-02 | Tampering (XSS) | freshness date / state text | mitigate | All text via textContent + createElement; freshness is a machine-formatted date, never a raw manifest field injected as HTML |
| T-07-03 | Denial (white-screen) | main.ts install catch | mitigate | Catch surfaces a visible empty state and never re-throws; the shell (map+header) stays up |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this plan (audit table: none); nothing to gate |
</threat_model>

<verification>
- `cd site && npx vitest run` (freshness + bottomSheet + all prior unit) green.
- `cd site && npx tsc --noEmit -p .` → 0 errors (no new runtime deps).
- `cd site && npx playwright test states responsive info -x` green; full `npm run e2e -w site` still green (no prior-phase regression) at wave close.
- Grep gates: `map.on("error"` present in init.ts; `showLoading`/`hideLoading`/`showEmptyState` present in main.ts; trust.css has `.bv-state` and NO `--accent`/`--score-`/`--chart-` references.
</verification>

<success_criteria>
- UX-05 initial-loading, map-load-error, and empty-stations states are wired at the three distinct seams and covered by active E2E assertions with the exact Icelandic copy.
- `freshness.ts` derives the newest data date client-side from `max(lastFetched)` (NO manifest/pipeline change) and formats an Icelandic date, tolerantly (missing → omit, never Invalid Date), unit-tested.
- `bottomSheet.ts` ships the 640px `MOBILE_QUERY` constant + pure `snapNearest`, unit-tested; drag controller stub typed for Plan 03.
- Wave-0 E2E skeletons for responsive + info exist green with fixme placeholders encoding every remaining UI-SPEC criterion + exact selectors.
- No new runtime dependency; tsc stays 0 errors.
</success_criteria>

<output>
Create `.planning/phases/07-responsive-ux-trust-states/07-01-SUMMARY.md` when done.
</output>
